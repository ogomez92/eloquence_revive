# What works and what does not

Last measured 27 August 2026.

## Works

The engine speaks, and it speaks IBM's samples. All 81 cases in six categories come out byte for byte identical to IBM's own binary: plain text, UTF-8, annotations, annotations with the annotation input type on, real-world text with the parameters read back in a person's units, and the user dictionary. That is English; eight of the SDK's nine languages do the same over the cases there are for each, and the sections below say which.

That holds in all four configurations the tree can build for this machine -- thirty-two and sixty-four bit, each with the rules run as bytecode and with the same rules run as the C they decompile to -- and in the Windows build as well, which is a fifth: `build/probe.exe` matches IBM's binary over the same 81 cases, under Wine on Linux and on Windows itself, where the scripts run both binaries without Wine in front of them.

Nothing is borrowed at build time. `make missing` answers nothing, which is the check that no call has quietly gone back to IBM's objects. The language data is all transcribed and in the tree, so a build needs a C compiler and Python and nothing else -- Python because the default build writes the rules out as C first. `make RULES=bytecode` wants the compiler alone and is half a minute rather than a quarter of an hour.

Dictionaries can be edited. `tools/delta-dict.py` writes `lang/enus/enus.dict` out of the tables and reads it back in, so a pronunciation can be changed, laid down and heard.

All nine languages in the SDK lift and decompile: US and British English, both Spanishes, both Frenches, German, Italian and Japanese. Eight of the nine build, speak and match IBM byte for byte over the cases there are for them -- everything but Japanese, which has an oracle now and is the subject of a section of its own: the engine below its romanizer is proved against IBM's, and the romanizer itself is what is left. English is the one that is finished in the fuller sense of having a dictionary a person can edit.

A build takes as many languages as it is given. `make LANGS="lang/enus lang/dede lang/engb lang/eses lang/frfr"` puts all five in one binary, and any other set the same way: `eciGetAvailableLanguages` answers with all of them, a caller picks one the way IBM's interface always allowed, and each is held against its own oracle out of the same binary. What made that possible is in `src/delta_lang.h` -- every module names its own tables after itself, because IBM gave them the same names in every language, and the engine reaches whichever is in force rather than linking to one by name.

That worked on Windows before it worked here. Any build with two languages in it died on Linux, in `sscanf` under `loadStandardConcatenativeVoice`, for a reason that had nothing to do with which two: joining the modules' settings blobs put one section straight behind another with no blank line between them, and `ini_getString` decides a key is absent by reading the byte where its search stopped rather than by where it stopped. With another section behind it, a key that is not there came back holding the next section's first value -- a dataset path where eight numbers were being read. The defect is IBM's, in their own `analysis/enus/win_iniread.obj`, and cannot fire in their engine: their `readFileIntoMemory` hands over one embedded blob for one language, whose sections are a blank line apart. Two sections butted together is a shape their data never has. `src/delta_lang.c` now writes that blank line and `src/eci_iniread.c` decides absence by where the search stopped; `make inikeys` is the check, and it needs neither Wine nor IBM's objects.

It builds and speaks on Windows, sixty-four bit, as one static file. The speak window plays what it makes through waveOut; `win/speak.c` is that, and it is the only front end that plays anything.

And it builds as `eci.dll`, exporting the fifty-two names IBM published, so a program written against IBM's library -- a screen reader add-on, most likely -- can load ours instead. Both bitnesses: sixty-four bit for an add-on that loads the engine into the reader's own process, thirty-two bit for the most used driver, which hosts the engine in a 32-bit process of its own whatever the reader is. Checked on Windows itself: by name from C for both, and through ctypes for the sixty-four bit one, as an add-on does.

What is not exported is the filter interface, which the engine does not implement, and the dictionary find, lookup and update calls, which have no public wrapper yet.

## Not done

Live audio on Linux. The engine hands its samples to the caller, and `build/evv` writes them as a wave file or down a pipe; nothing sends them to a sound card as they are made. Windows got there first because waveOut is forty lines; PipeWire is next and is a thin sink on top of the same buffer.

Polish, which is what all of it is for. It is started: `lang/plpl` is the ninth language in the tree, it builds, it speaks, and it is Italian. The section below says what that means and what is left.

Japanese, which is the one language still not built. Everything else in the SDK now is.

## German

German builds and speaks, and speaks IBM's samples. `make LANG=lang/dede probe` and the reference beside it are in `docs/building.md`, and `EVV_LANG=dede test/suite.sh` runs the same six categories over 80 cases of its own. On 22 August 2026 all 80 came out byte for byte identical to IBM's German binary.

That was the sixty-four bit Windows build, running on Windows against a reference built from `analysis/dede`, with the rules run as bytecode and with the same rules run as the C they decompile to; and both on its own and linked beside English in one binary, where each language still matches its own oracle over its own cases. Linux followed on 23 August 2026, again in both rule forms, on its own and in a binary with English and British English in it, and thirty-two bit the same day in both rule forms -- all 80 every time. So German now passes everywhere English does.

Two things had to be fixed to get there, and both were ours rather than the language's.

The first was the table of which dictionary is in force for which language. It holds a four-byte slot per language and dialect, and it was being written as a host pointer, which on a sixty-four bit host reaches over the slot beside it. English never showed it: family one is the first slot the sweep looks at, so the slot its store reaches into is one nothing ever reads. German is family four, so the sweep read the slot the store had reached into and handed the engine half a pointer, and every German dictionary case crashed. `src/eci_dict.c` keeps a value there now, as the original does.

The second was in the lifter, and it was one wrong number in one rule. Where the compiler wrote a small constant into a register as `push` and `pop`, `tools/delta-lift.py` folded the pair into a single load. A switch arm that wants a different constant in the same register is written as its own push and a jump to the pop of the arm below it, so that pop is a landing place, and folding it away took it out from under the jump: the arm that jumped arrived past it and the register kept whatever it happened to hold. In the German `/r/` that put the alternative's own number into the fricative amplitude -- 82 where IBM says 45 -- one of the hundred and thirty-one points the rule writes into its streams for `tra`, and the only one that differed. It is what made a German word with certain consonants before an `r` wrong -- `tra`, `kra`, `pra`, `gra`, `fra` differed and `bra`, `dra` and `ra` did not -- and, through the rules that name a character, every case with a backtick in it.

The lifter no longer folds when something jumps at the pop. Lifting again changed three places in German and two in English, which is why `lang/enus/delta_rules_enus.c` moved as well: the same pattern was there all along and no English case had ever reached it. English still passes all 81.

What is left is not German's. Two of the cases with markers in them -- an audio marker and a pair of index marks -- differ from run to run, and it is IBM's binary that varies: over six runs of one of them ours produced the same samples every time and the reference produced different ones once. `test/compare.sh` already retries a case that hangs, which is how the same flakiness shows up in English, where it hangs instead of answering differently.

The NVDA add-on follows. Where the library it loads has more than one language in it, every language's eight presets are offered as voices of their own -- "German - Voice 3" -- each saying which language it is, so the reader matches a document's language to one of them; and a `LangChangeCommand` in a speech sequence switches the engine mid-utterance, so a German quotation in an English page is read as German. A library with one language in it offers what it always did, under the same names, so nothing a reader had chosen is lost.

Settings are queued as control steps rather than as speech. Anything queued as speech is thrown away by a cancel that overtakes it, which is right for an utterance and wrong for a setting: every keystroke cancels, so a rate or a voice chosen at the wrong moment simply did not happen, with the dialog still showing what had been asked for.

A watchdog breaks a stall in the wave player. Blocking in the player is how the add-on paces synthesis -- a full player is what keeps the engine from running ahead of the speech -- but the engine is spoken to from one thread, the player has no timeout, and a player that has stopped draining looks exactly like a full one. So it held that thread, and every utterance queued behind it, saying nothing and writing nothing down; speech came back only when something else asked for silence and stopped the player as a side effect. A wait longer than five seconds where no pause was asked for is now logged, the utterance abandoned and reported finished so nothing is left waiting on a mark that is never coming. A pause the reader asked for is left alone, since a paused player is meant not to drain.

Long text is handed to the engine in pieces, so that asking for silence waits out a piece and not a whole message. The engine cannot be interrupted -- that is settled and is written up above -- so a cancel costs the synthesis time of the utterance in flight. For a line of a list that is 0.03 s and invisible. For a chat message it is not: measured on the shipped engine, one message of 3,222 characters costs 0.83 s, and 560 characters of Arabic, which is spelled out character by character into six minutes of speech, costs 1.44 s. A reader arrowing a list every 200 ms has every item that lands inside that wait dropped, which is speech going silent for several objects and then catching up. A piece ends at sentence-final punctuation, where the engine already pauses: a measured 300-character message took 252,010 samples whole and the same 252,010 samples in six sentence pieces. A dot has to argue for itself, since a boundary the engine would not have made costs that pause while one declined costs only a longer wait: a word with a dot inside it and a short word starting with a capital are an abbreviation and an initial rather than a sentence, which is what keeps "Mr. Jones" and "J. R. R. Tolkien" in one piece -- cut at the dots they measured 0.70 s and 1.48 s longer than whole. A stretch with no sentence end falls back to whitespace after 500 characters, which keeps the unpunctuated case bounded without putting full-stop pauses through ordinary prose. No piece ends while spelling or prosody is open, so cancelling cannot drop the command that restores the engine's state.

Not done for German: no dictionary in a form a person can edit, since `tools/delta-dict.py` has only been run for English, and no `long` cases.

## British English

British English builds and speaks, and speaks IBM's samples. `make LANG=lang/engb probe` builds it and `EVV_LANG=engb test/suite.sh` runs the same six categories over 81 cases of its own, against a reference built from `analysis/engb` -- `make -C reference TAG=engb BUILD=../build/reference-engb`. On 23 August 2026 all 81 came out byte for byte identical to IBM's British binary, in all six builds -- sixty-four bit, thirty-two bit and Windows, each with the rules as bytecode and as the C they decompile to -- and again out of a binary with English and German in it.

It is family one dialect one where US English is family one dialect nought, which is the first pair of dialects in one family the language mechanism has carried; `test/compare.sh` knows it as `0x10001`. That is the thing British English proves which German could not: nothing keys off the family alone.

It matched on the first run, with no fix needed anywhere -- the lift, the mechanism and the tools were all already right. What it did find was the multi-language crash above, and only because two dialects of one family was the pair being tried at the time; English and German turned out to crash identically.

Not done for British English: no dictionary a person can edit, and no `long` cases of its own.

## The Spanishes, the Frenches and Italian

All five lifted and matched IBM byte for byte on 23 August 2026, over 81 cases each: `EVV_LANG=eses`, `esus`, `itit`, `frfr` and `frca`, each against a reference built from its own objects. Their language numbers are 0x20000 and 0x20001, 0x50000, and 0x30000 and 0x30001, and `test/compare.sh` knows them all.

Four things had to be fixed to get there, and every one of them was ours rather than the languages'.

**A compound variable of the seventh kind sits on a four-byte boundary.** The two Spanishes, French of France and Italian could not be lifted at all: the model of the variable area came out two bytes short at one compound. It was not the payload rounding, which had been the standing guess and which breaks the languages that already worked. It is alignment -- a compound whose first word is 6 holds four-byte items and is laid four-aligned, and every other kind two-aligned. Across all nine modules every one of the thirty-six is four-aligned and no other kind's are. The rule lives in two places that have to agree, `tools/gen-globals.py` for the lifter's model and `src/eci_deltaglob.c` for the area the engine really builds, and fixing only the first left the engine reading a variable's kind out of the middle of the cell before it. What says it is this rather than merely sufficient: every one of the eight built modules regenerates its variable area byte for byte from the objects, the three that were committed before this included.

**A jump names a place in its rule, and the place was being read as a signed number.** So it wrapped at 32,767. English's longest rule is 30,929 bytes -- within six per cent of that and never over it, which is why three languages never showed it. French of France has a rule of 33,075 bytes and Canadian French one of 34,154, and both jumped somewhere negative and took the machine apart, by way of a rule finding its own activation record above the live frames. Nothing about the bytecode changed: the emitter always wrote a position from the start of the rule, which cannot be negative, and only the reading of it was wrong. The emitter now refuses a rule too long to name a place in rather than writing one that cannot be read.

**Two machine primitives no English rule reaches.** `lpta_loadi` loads an immediate into the left accessor, asking the statement table for the kind instead of reading it off a location; all five wanted it. `insert_rv` inserts a rule's variable over a range taken to the right, and only Canadian French wants it -- it is `insert_2ptv` with a different range call, whose answer means the opposite way round.

**A little floating point, which is the surprise.** Two rules in France's module and eight in Canada's use the x87 stack: an integer pushed, multiplied or added to by a double constant or another integer, and truncated. Nothing else in nine languages does, which is what one would expect of a fixed-point engine. It is worked out in `long double` because that is the register the original computes in, and the difference is not academic: with the constant 0.4, an input of 5 and an addend of -3, sixty-four bit arithmetic keeps 2.0 exactly and truncates to -1 where the eighty-bit register keeps 2.000000000000000111 and truncates to 0. Two of two point nine million combinations differ and that is them.

One thing about the case files is worth knowing. The engine is a single-byte engine, so the `utf8` cases really test that both sides mangle multi-byte input the same way rather than that either handles it. For Spanish that is not merely mangled: an o-acute directly before an n crashes IBM's engine and ours identically, at the same fault address, so `razón` cannot be compared at all and the case file avoids it. The same text in Latin-1 speaks perfectly.

Not done for any of the five: no dictionary a person can edit, and no thirty-two bit or Windows build.

## Japanese

`docs/japanese.md` is the whole of it, written for somebody picking this up who was not the person who found it: what is done and proved, what is left with its sizes, the two harnesses, the faults they found, the oracle and why it can be trusted, the decisions already taken, and the traps. What follows here is the short version.

Japanese lifts -- 477 rules, its settings, and the language number 0x80000 -- and what stands between that and a language that speaks is the romanizer, which is what `jpnrom.dll` is in stock Eloquence.

**Everything below the romanizer is now proved right, which it was not before.** Japanese text goes through the romanizer and comes out as a phoneme string with prosody annotations in it; from there the engine that speaks the other eight speaks Japanese. `reference/romtap.c` records every call IBM's romanizer manager makes and every answer it gets, and `test/romcan.c` replays those answers as a romanizer with no Japanese in it. Our samples are then IBM's, byte for byte, over seven Japanese cases -- kana, kanji, katakana, numbers, embedded English, romaji -- and the conversation with the romanizer is identical call for call. So Japanese is a text-to-text problem now, with an exact oracle in front of it.

**The seam is transcribed and the vtable slots are gone.** This project used to say that finding a romanizer was Win32 `LoadLibrary` work; it is not. IBM takes the address of `getRomObject`, a link-time symbol its own `romedll_link.obj` answers, and the only Win32 in it is asking where the program was loaded from. `src/eci_rom.h` says what a romanizer is -- one struct of named functions where IBM had numbered slots -- and `src/eci_romedll.c` stands in for the linker's answer. A romanizer is a property of its language, and `rom/<tag>` is built exactly when `lang/<tag>` is, so an English build carries none of it.

**The data is lifted, in two commands.** `tools/lift-rom.py` takes the static dictionary: 1,723 blobs and 2,669,092 bytes. `tools/lift-romtables.py` takes the five objects whose code is a few accessors over a great deal of table -- `dictman`, `unicodeconvt`, `jpnutil`, `userdict` and `phrasebuf`: 115 tables and 193,858 bytes.

**Thirteen files are written and held to IBM's answer.** `rom/jajp/rominstparam.c` is the parameters and errors, proved by `EVV_ROMCAN_PARAMS=real test/romcan.sh`, which hands that half of the recorded conversation to it, and `rominstance.c` is the instance and its forwarding. `unicodeconvt.c`, `dictman.c`, `jpnutil.c`, `dictsearch.c`, `userdict.c`, `annotation.c`, `inputchar.c`, `codeconv.c`, `inputmngr.c`, `convtinterface.c` and `phrasebuf.c` are proved by `test/romprims.sh` over 789,549 calls a side: every byte and every two-byte character through every predicate, all 65,535 code points converted both ways, every accessor over the whole range of the table it reads, every kana code into letters, every hiragana into katakana, the whole of the run-to-readings walk over twelve Japanese texts at every position of each, a user dictionary taught eleven words and looked up against a sentence in both of the modes there are, the reader's own record printed byte for byte after every call that writes it, all 65,536 two-byte characters through the classifier that decides what each one is, every one of the 65,536 pairs through the JIS-to-Shift-JIS arithmetic and through the half-width widener in both of its walks, and every byte of the alphabet in each of the eight contexts that decide a road through the two whole-text codeset conversions.

**The annotations are lifted out and put back.** `rom/jajp/annotation.c` is the whole of `Annotation`, six methods: the marks a caller puts in the text are not Japanese and must not go through the analyser, so they are kept in a ring of 128 with the position each belonged to and written back as the output passes that place.

**The user dictionary works.** `rom/jajp/userdict.c` is the whole of `RomUserDict`, eleven methods: a caller teaches it a word as written and a reading in kana with a caret where the accent falls, and it normalises both into the forms the built-in dictionary uses, keeps them in the same skip list the English dictionary uses, and writes what it finds into `DictSearch`'s candidate entries so that a taught word is indistinguishable from a found one. All fifteen sabotages of it move lines. It also found a ninth deliberate divergence, and this one is IBM's: `updateDictExt` allows a key twice as long as the buffer it builds it in, and a word of twenty half-width kana walks off its own stack. Measured at exactly twenty.

**The first real piece of the analyser is written.** `rom/jajp/dictsearch.c` holds all sixty-two of `DictSearch`'s methods, and they are the closure of `GenerateWord`: take a run of text, work out every way it can be read, and look each reading up in the dictionary. That closure calls nothing outside itself that was not already written, which is what made it the right unit. Two record formats came out of it and four more regions of the map were settled, including the long-reading store in the spine at 0x5f2. All twenty-two sabotages of that closure move lines. The number reader went in after `InputChar` did, and then `Do` itself with one private method of `Romanizer` to close it -- so the search runs now: text goes in as characters and comes out as every way the sentence can be read. The class is whole now: the four function-word methods that walk a phrase backwards went in last.

**The first piece of the analyser above the dictionary search is written.** `rom/jajp/phrasebuf.c` is all nine methods of `PhraseBuf`: where the path search's answers become accent phrases, one slot of 344 bytes for each way the sentence can be broken up, holding the words, their readings, the accent and what kind of phrase it makes. It closes on code already written, which is what made it the unit. Three record formats came out of it -- a path, a sub-word and a phrase -- and `rom/jajp/phrasebuf.h` and `rom/jajp/jpath.h` are the maps, both held against IBM's objects by `tools/rom-offsets.py`, which now checks five records rather than three. Fifty-seven of sixty-one sabotages move lines and `docs/japanese.md` names the four that do not. The sweep found two faults in the reading: the cost of a phrase comes from the first word on its path rather than the last, and the test that refuses a one-word path reads how many characters it covers rather than how long its reading is.

**The surface is written, so text now arrives the way the engine sends it.** `rom/jajp/convtinterface.c` is all twenty-one methods of `ConverterInterface`, `inputmngr.c` all ten of `InputManager` with the three queue-element classes beside it, and `codeconv.c` the five conversions of `JpnUtil` that IBM keeps in an object of its own. Text is recoded into Shift-JIS if it arrived in EUC-JP or in one of the three seven-bit JIS codesets, and waits with the `InputManager` until something asks for it; a mark or a parameter set part way through goes on a queue with a note of where in the output it belonged; and the user dictionary calls pass through to `RomUserDict`, so the ECI dictionary calls no longer answer refused for Japanese. `ConverterInterface` is Romanizer's base class, so `rom/jajp/romanizer.h` is no longer a guess at its head: the eight fields from 0x00 to 0x1f are read out of `convtinterface.obj`, and every displacement on a pointer in that object is one of them. A hundred sabotages were tried and eighty-one move lines; the eighteen that do not are named one by one in `docs/japanese.md`, and they are memory rather than answers, allocations that cannot be made to fail, or roads that read nothing the sabotage changed. Writing it corrected something this tree used to say, that `RomInstParam::setInputType` has no caller at all -- it has exactly one, `addText`, so `getParam(0)` and `isAnnotationsInText` answer what the last text was said to be rather than always nought.

**The reader is written, and it is the whole of the analyser's first stage.** `rom/jajp/inputchar.c` is all twenty-one methods of `InputChar`: text arrives as bytes and leaves as three parallel arrays of seven hundred and twenty-six -- the characters, what each one is, and where each began in what the caller actually sent -- read one sentence at a time. `rom/jajp/inputchar.h` is that record, and `tools/rom-offsets.py` checks it against `inputchar.obj` the way it already checked the other two.

`GetCharType` is in it, which is the method every earlier unit's character tests were read off, and it is now swept over all 65,536 two-byte characters against IBM's own. So the twelve kinds are settled by measurement rather than by reading.

Two faults of the map came out of writing it. What `DictSearch` calls the user-dictionary context is one of the reader's own chain nodes, and the written form it names is a pointer at IBM's fourth byte, which on a sixty-four bit host it cannot be; two files were reading eight bytes there. And `IC_OFFSET` is not an offset into the buffer at all but into the raw text `TextAnalysis` keeps, which is how an annotation gets put back where the caller wrote it after the reader has folded and rewritten characters on the way through.

**Both harnesses found real faults on their first run**, which is the argument for building them before the romanizer rather than after. `getActiveSampleRate` in `src/eci_managers.c` answered nought, so the sample rate never reached a romanizer -- and no language could see that, because the romanizer is the only thing in the engine that asks. Three faults are IBM's own and are not reproduced: `MBCSToUCS2` loops for ever on a byte of 0x80, 0xfe or 0xff; it looks a pair beginning 0xfd up past the end of the table it looks it up in, answering with whatever the linker put next; and `Hiragana2Katakana` walks for ever on 0x82ec, hiragana small wa, which its table has not got and its own test for hiragana lets through. That last one is measured: with it in the sweep, IBM's side does not finish in sixty seconds.

What is left is the analyser above the dictionary search and the output side below it: 169 entry points in ten classes, counted from `nm` across the whole directory, which is roughly a quarter of the romanizer still to write by that measure. `TextAnalysis` is 37 of them and is the spine; `MakeReadableJP` 32 and is what produces the phoneme string; `Romanizer` 17 of its 18 and is last rather than next, because it drives the other four. `docs/japanese.md` has the order, which comes from the graph of which unwritten class calls which, and says what finishing it means besides the transcription: `JPROM_INCOMPLETE` out of `rom/jajp/jprom.h`, `lang/jajp` into the tree, and `EVV_LANG=jajp test/suite.sh` against a Japanese reference, for which the cases already exist.

The one thing to know before starting on it: nothing is audible until nearly all of that exists, because the phoneme string is made by the last two pieces. Every unit before them is proved against IBM byte for byte and heard by nobody.

`lang/jajp` is deliberately not in the tree until it can make an instance, since a module that cannot would break any build that named it, and `.gitignore` says so. It lifts again in one pass from the tools. `rom/jajp/jprom.h` defines `JPROM_INCOMPLETE` while the romanizer cannot convert anything, which makes it answer no instance at all rather than speaking something wrong.

## What the lifting cost, across all nine

All nine lift their rules cleanly, which was the part expected to be hard, and
the holes that remained were smaller than the headline suggested. Every one of
them turned out to be a gap in our machine rather than something not understood
about the language: two primitives no English rule reaches, one alignment rule,
one signed number that should not have been signed, and a little floating point
nobody expected a fixed-point engine to contain. The sections above say which
was which. What is left is Japanese, and what is left of Japanese is a
transcription rather than a question: it has an oracle, the engine below its
romanizer is proved, and the romanizer is measured.

## The compiler

A rule can be written now, and two of English's own have been written that way to prove it.

`lang/enus/rules/*.up` is a rule as what it does: every call the same entry with the same arguments in the same order, and the machine's own bookkeeping -- the argument stack, the flags, where in the frame a local sits, the landing place, the entry, the two tails and the numbered dispatch a rule backtracks through -- left to `tools/delta-upper.py`. That is where the length goes: two thirds of every rule in the lower notation is the argument stack written out by hand. `eng_ph_F_dur` is 49 lines of it and 5 of the upper form, and comes out with the frame IBM gave it.

The standard it is held to is not byte-identity, and that is a decision rather than a shortfall. Matching IBM's bytes would mean making the same register choices and emitting the instructions in the same order as its compiler, which is a study of that compiler rather than of this engine, and it would forbid writing anything IBM never wrote. What is required instead is that the engine cannot tell the difference: `make upper-check` speaks the seven plain cases through a build carrying the authored rules and through one carrying IBM's, and holds every rule entered and every call made with its arguments against each other, and the audio besides. `eng_ph_F_dur` and `has_lex_prefix` -- the second being two alternatives, a tail they share and a dispatch through six planted places -- come out the same over 6,811,159 lines of trace.

The audio in that comparison is not the weak half of it. A rule whose whole effect is to write a variable makes no call that shows it: setting `eng_ph_F_dur`'s duration to 21 where IBM sets 20 passes the trace on every sentence and changes the sound of the second.

A rule of ours can also name bytes of its own, which is what a text rule needs and what nothing but IBM's objects could supply before. `lang/<tag>/rules/constants` holds them, `make constants` lays them down in the one file in a language module that no lifter writes, and startup copies that store into the arena beside the lifted ones. `docs/building.md` says how the naming was proved as against the linking.

What is left in the compiler is the ergonomics rather than the reach. Every operation can be named, the rare ones through a line of the lower notation; what a person writing several hundred Polish rules would want on top of that is the language's own idioms said shorter -- an alternative as a block rather than a place and a plant, and a run of context tests as a run of tests. Naming an operation is not the same as the engine having it, which is the section below.

## The machine's own reach

The Delta machine was in this tree only as far as the nine languages IBM shipped ever reached into it. Its seventeen objects define 444 names, and 136 of those had no answer here, not because they were hard but because no rule calls them, so the link never asked and nothing was ever reported missing. A rule of ours can ask, and then it was missing. That is over.

All 136 are now written, the last sixteen on 27 August 2026, so every name the seventeen objects define has an answer here and a rule of ours can ask for anything the machine has. The ones a rule being authored is likeliest to want came first: every arithmetic operation beyond addition, all of `test`, all of `assign`, all of `stack`, all of `pointer`, all of `optimize`, all of `for`, and every string test the machine has, the two that hold the scan where it stands, the context and stream calls a text rule reaches the spine through, and the walk over the names a field can take. `docs/remaining.md` has the list, object by object.

The last sixteen were the ones no rule can reach, and they were set aside as declined before being written anyway. The largest is `vgen`, a thousand lines that writes a generate statement out, with `generate` and `vgenerate` above it and the `val_expr` chain below -- the chain `src/eci_deltamisc.c` had declined long before this work, for the reason then written at its foot. The rest are the debugger's consistency check, `merge_sync`, the program entry this engine does not want, and the two the save layer and the stack reader would use. Two of the number it all started from were the census miscounting a fastcall name rather than a gap, which `docs/remaining.md` says too.

They are proved rather than argued for. `test/prims.sh` compiles one table of cases twice -- against our engine, and against IBM's own objects, which define these under plain C names -- and diffs what each call leaves behind: 52,159 calls, identical. That is the differential harness put back for the one thing the suite cannot do, since a call no sentence reaches cannot be checked by speaking.

The machine it calls them on is a real one, and both sides build it the same way: `delta_new`, the command layer, the streams, the language's start rule, a sentence handed to the link and read in. Every step of that answers identically on the two sides, which is what makes them the same machine rather than merely similar machines. What is on the spine is compared without being decoded -- every code is offered to the string test and the ones that match are printed -- so a position holding the same character on both sides says so without the harness knowing what the character is.

Three of the last sixteen are compared there as well. `set_saved_ptrs` is given a word variable and two pushed locations carrying the same made-up value and asked to move one of them, and `ins_rdtoks` is built the stack it wants -- a bottom marker with two pushed values over it -- and lays a token for each, which is what reaches `vins_tok` and `vins_sync`. `vgen` is given a cell built for the occasion, and what is compared is what it answers and the two marks its first pass settled on; the frame loop wants a spine with statements on it, so it is not entered. The other thirteen are read rather than compared, and `docs/remaining.md` says what each would need to be reachable at all.

Not everything needs a spine, which is why the last batch went in quickly: the walk over a field's declared names, the uniqueness test and the two prefix tests all read the language's own table, and the harness prints what they answer in full rather than a landmark -- the alphabet, `undefined lower upper`, which characters could still start a name.

The spine is compared as a shape: every node is asked the string test for every statement kind and every code, and the pairs that answer are what is held against the other side. That is how an insert is checked -- the four widths leave four different shapes and the two engines agree on each -- and it is worth knowing what it does not reach. The value a wide insert decodes is invisible, since the nodes it makes answer no test at all. `get_tok` leaves two nodes rather than a sentence, so a walk has little room and the walking branch of the context pair is never taken. And three of the stream calls cannot be compared at all: IBM's own `vsplit_time` faults on the only position the harness can offer it.

Every one of those is written where it bites rather than left for a passing count to imply otherwise.

Three things reading turned up. The immediate loads into the right pointer register ask the statement table about the *left* register's field, which is a slip in the original that cannot show, and is kept. All four `settvar` entry points are the same body: none looks at the width its name announces. And so are the two that ask whether a context may be taken, which follow the same word of the node whichever side they are named for.

## Polish

`lang/plpl` exists and speaks. As it stands it is Italian under a Polish name -- made by copying `lang/itit`'s five text forms and its rules and renaming them -- and that is the chassis rather than a placeholder: from here every change has something audible on both sides of it. `NOTICE` says the licence consequence, which is that all of it is IBM's Italian data until it has been replaced.

It was built from the text forms and nothing else. No object is opened after the template's rules are written out, which is what those forms were for. It speaks the same samples as Italian, to the hash, on the same sentence; it holds up in one binary beside the other eight, each of the nine still saying exactly what it says alone; and English's 81 cases and samples are untouched.

Italian is the template on phonetics rather than convenience. Its stress is predominantly penultimate as Polish's is almost always penultimate, its five vowels have no reduction, and its consonants carry the affricates ts and dz, tʃ and dʒ, the palatal nasal that is exactly Polish ń, and a trilled r. Spanish has half of that. What no module has is the vowel y; the nasal vowels can be read out of French and /x/ out of Spanish, since all eight are text now. The alveolo-palatals ś ź ć dź turned out not to be missing at all -- Italian's own S and Z are marked palatal in IBM's phoneme table, which is what that series is -- and what had to be found was somewhere to put the hard series instead.

Polish is family seventeen, `0x110000`. The family is not free: three tables are indexed by it and hold eighteen, IBM used six, and four more -- 6, 10, 11 and 16 -- are families its own `rz_isRomExist` says have a romanizer, so an instance of one is refused outright when the romanizer is absent. Family sixteen was tried first and `eciNewEx` answered -21.

The first rule written for Polish rather than lifted for Italian is in `lang/plpl/rules/is_val.up`: a locus rule, written in the upper form against names now carried in `plpl.globals`, where ten of the machine's variables are labelled as the formant targets a consonant is spoken with.

`make EVVLANG=lang/plpl census` says how much of the module is still Italian: 1,749 rules of 1,750 character for character, the settings two lines apart, the variables named. What it prevents is Italian phonology coming out of something labelled Polish without anyone noticing.

Polish's own letters are in it. A language's alphabet is the value names of the input statement's first field, and beside it in the same statement is a record of five bytes for each of those names: the character's case, whether it is a letter, whether it is a vowel, whether it carries an accent, and the phoneme it says on its own. That last is letter-to-sound at its simplest and it is data rather than code, which `tools/lang-alphabet.py` now reads and writes by name.

The sixteen Polish letters went in at byte values the alphabet did not claim -- nineteen were free and sixteen were wanted -- rather than by reusing a code, since the dictionaries are keyed by code and moving one moves every word that used it. Each starts from the nearest phoneme the module has: ł says w, which is what Polish ł is, ń says the palatal nasal Italian spells gn and Polish spells ń, ć says C, ś says S, ź and ż say Z, ą says a and ę says e until the nasal vowels are read out of French. ó was already there.

Measured rather than assumed: before, a Polish letter cost about thirteen thousand samples wherever it stood, because the engine had no name for the byte and read it as a symbol -- eight of them alone came to 103,356 samples. After, `kąt` is 10,648 samples where `kat` is 8,107, and the two traces share their first 14,336 rule entries. A Polish letter on its own is still silent, because a lone letter is spoken by its name and Polish's letters have no names yet.

And Polish text arrives now. A caller writes code points and the machine reads single bytes, and IBM's engine does almost nothing between them: the code set only mattered under the SSML filter and for the four families with a romanizer, so on the ordinary path the caller's bytes are the characters. That was enough for the nine languages IBM shipped, every letter of which is in the Windows Western byte set, and not enough for one whose letters are not. `lang/<tag>/<tag>.codepoints` says what each of a language's own characters arrives as, `make codepoints` writes it into the module, and `addTextRun` converts on the way in.

That is a deliberate divergence, the fourth in the tree, and the guard is what makes it safe rather than merely careful: the conversion runs only for a language that declares characters of its own, and the nine IBM shipped declare none. What says so is not the argument but the suite -- English's 81 cases, German's 80 and the samples hash, on sixty-four bits and on thirty-two, all untouched.

Measured on the same pangram: `Zażółć gęślą jaźń` is 16,819 samples where it was 120,714, and `kąt` written as UTF-8 comes out byte for byte identical to `kąt` written in the module's own bytes, which is what says the conversion is exact rather than close.

The first change to how Polish sounds is in as well, and what it proves is worth more than what it does. Polish speaks sz, ż, cz and dż as retroflexes, further back than the palato-alveolars Italian spells with sc and gi, and a retroflex is a low third formant. `pol_retroflex_Fv` in `lang/plpl/rules/is_val.up` says that -- f3 down from Italian's 2400 to 2200, f2 from 1800 to 1700 -- and the two calls inside `ital_ph_S` and `ital_ph_Z` in Polish's own copy of the rules point at it. `sciarpa` spoken by Italian and by Polish is the same word at the same length, 10,197 samples each, with 17,448 of its 20,438 bytes identical: exactly one sound moved. That is the whole path from an authored rule to the samples, proved.

Two letters that make one sound are in, which is what a Polish word needed. Polish spells its retroflexes as digraphs -- sz, cz, rz, dz and dz with a dot -- and `lang/plpl/rules/it_phone.up` now has a rule for each of the five letters those start with, written in the upper form, with the chain in `it_phone.dr` that hands a letter to its rule pointed at them one call at a time.

The idiom is the machine's rather than ours. A letter rule is handed the two ends of the run it is being asked about and fills the range between them with phonemes, so a rule that says two letters make one sound moves the right-hand end past the second letter first -- `savescptr`, which puts where the scan has got to into the cell -- and answers that position back to its caller through the second pointer, so the next letter read is the one after the digraph. Italian's own rules do exactly that for ss and sci, which is where it was read from.

What it changes, word by word. `szafa` was s z a f a and is now the retroflex, a, f, a. `czas` was k dz a s -- Italian reads c before a consonant as k -- and is now the retroflex affricate, a, s. `rzeka` was r ts e k a and is now the voiced retroflex, e, k, a. `dzwon` was d dz v o n and is now dz, v, o, n. `dzem` with a dot was d, nothing, e, m and is now the voiced retroflex affricate, e, m. Polish c on its own is the affricate Italian spells z, so `cena` was tsh e n a and is ts e n a; Polish z on its own is a z, so `zima` was dz i m a and is z i m a. ch stays a k, which is what Italian does with it and as near as this inventory comes to Polish /x/, so `chleb` is k l e b.

And `las zima` keeps its s and its z apart. The scan that reads the next letter stops at the fence, so a word ending in s before one beginning with z is two words and not a digraph. That is `setscan_nof_r` rather than `setscan_r`, and it is the difference between one right answer and a rule that would have run words together for ever.

The eight letters Italian has not got speak now as well, and getting there took three things.

The first is that they arrive at all. They did not: IBM's romanizer runs every byte of the text through one 256-byte table when no corpus is loaded, and that table turns most of 0x80 to 0x9f into a space, those being undefined in the Western set it was made for -- which is exactly the range a new language's letters are free to take, since everything else is claimed. So a Polish word arrived as several one-letter ones and every Polish letter was a space: `dzem` with a dot came out as the letter d spoken by its Italian name and then em. `rz_convertText` now leaves a byte alone if the language in force claims it in its own `codepoints`, and that is the fifth deliberate divergence in the tree. The nine IBM shipped claim none, so for them the table decides as it always did.

The second is a link in the chain. A letter reaches its rule through a chain of alternatives in `generate_ital_diaphons`, one per letter: the chain sets the scan at the letter, asks whether it is this letter, and calls that letter's rule. A letter Italian never had has no link, and adding one means renumbering the tags the whole 1,057-line rule backtracks through and extending the table it dispatches on. So the link for q, which Polish does not use, is pointed at two rules of ours instead: one says whether the letter is any of the eight, the other speaks it. Eight questions in a row about the same letter cost nothing, because a one-character test that fails does not move the scan.

The third is that a rule standing where a wrapper stands has to be shaped like one. The first version was an ordinary rule -- landing place, `ventproc`, `succeed` -- and it committed the choice points the chain had planted around the call. Nothing was visibly wrong until a word with no vowel in it crashed several rules later in the durations. `bare` in the upper form is the answer: entered, and it answers, which is what all 1,037 of the language's own wrappers are.

What the letters say: l with a stroke is w, which is what Polish l with a stroke is; n with an acute is the palatal nasal Italian spells gn; s with an acute is the retroflex and c with an acute the retroflex affricate; z with an acute and z with a dot are both the voiced retroflex. The two ogoneks are nasal vowels, which this inventory has not got, and are spoken as the vowel and then an n -- which is what Polish itself does before a stop. Measured: `kat` with an ogonek is 8,712 samples, exactly what `kont` is, where `kat` is 8,107. `zaba` with a dot over the z is the same length as `zaba` without and a different sound, which is one letter moved and nothing else. `Zazolc geslq jazn` written properly says every one of its letters and is 20,240 samples where it was 16,819 with six of them silent.

Six of the eight go through wrappers the module already has, so a trace says which phoneme was laid down. The two ogoneks lay down two phonemes and no wrapper carries a pair, so they name their bytes in `lang/plpl/rules/constants` and call the primitive.

All three shapes say it the same. The thirty-two bit build and the Windows one agree with the sixty-four bit build byte for byte on `szafa`, `zaba` with a dot, `kat` with an ogonek and the pangram -- but only when the text is handed over in a file. On the command line the Windows build disagreed on every case with a Polish letter in it, and `zaba` with a dot came out as exactly the hash of `zaba` without: Wine converts a command line to the Windows code page on the way in and that page has no Polish. It is the harness and not the engine, and it is written down here because it reads exactly like a bug.

One fault is left and it is understood rather than guessed at. A lone Polish consonant -- a bare z with a dot, or s with an acute -- crashes. A word with no vowel in it is not spoken by the letter rules at all: it is spelled out letter by letter, which is why `psst` comes out as pi ti and `sz` on its own as zeta. A letter of Polish's own has no name to be spelled with, so the word ends with nothing to say and the durations are asked to shape an utterance that is not there. Every word with a vowel in it is fine, and so is any word whose first letter is one of Italian's. Two null guards in the machine were tried and taken back out: each moved the crash one dereference further on, which is what says the state is wrong rather than the pointer. The fix is the letter names, in the dictionary, and that is the next piece of work.

While the letters were being put in, `tools/lang-alphabet.py` was found to have added every one of their records one slot too high. The alphabet is 207 names with 208 records, IBM's own trailing spare being the extra, and `add` was appending after that spare rather than before it -- so the sixteen Polish letters each read the record of the letter before, and the first of them read the spare, which says undefined for everything. Nothing had noticed because nothing yet read those records for a sound. The tool inserts before the spare now, and `lang/plpl/plpl.statements` has been repaired with the same bytes in the right order: l with a stroke says the glide again, n with an acute the palatal nasal, and the ogoneks their vowels.

There is no oracle for any of it -- IBM never made a Polish module -- so the ear is the judge, and a hash beside each verdict is what will keep an approved sound from moving afterwards. `tools/lang-phonemes.py` is the map: every phoneme as the statements name it, the settings declare it and a rule speaks it, with the place each one is spoken at. Italian declares 35, gives 21 a rule, and `ital_pal_Fv` serves four of them -- which is the sort of thing to know before moving any of it. The tool that would let the ear be spared the structural part is `build/probe ... p`, which asks for phonemes instead of sound; it is one flag away from working and `docs/building.md` says which. Until it works, a trace read for the insert wrappers says what a word came out as, which is how every phoneme above was measured.

Then the two sibilant series were separated, which is the change with the most Polish in it so far.

Polish has two where Italian has one: the hard sz ż cz dż, said with the tongue curled back, and the soft ś ź ć dź, said with it flat against the palate. They are different letters and different words -- kasza is not Kasia -- and both were coming out of the same two engine sounds, so cześć had the same noise twice.

IBM's table has the distinction already. Every phoneme carries a record of eight bytes -- class, voicing, sonority, manner, place, and the three a vowel wants -- and place runs lab, alv, pal, vel, ret. Italian never uses ret, and its S and Z are pal, which is what the soft series is. So the soft series is Italian's own two as they stand and the hard series needed two phonemes of its own.

There was nowhere to put a new one, and that is the finding worth keeping. A phoneme code is tested by name in some five hundred places across a module: `ital_labial_Fv` alone asks about its neighbours 68 times, each vowel rule asks which consonant follows it upwards of fifty, and the durations, the transition durations and the syllabifier all ask. A code that has never existed is invisible to every one of them -- its neighbours coarticulated as if it were not there, its durations from nowhere, which is the same class of hole that crashes a lone Polish letter. A code that already exists has an arm in every chain.

So the hard series took over the two of Italian's that no Polish word can reach: L, its palatal l from gli, and R, its strong trill. Measured rather than assumed -- glina, kolano, lala, rok, brat, burza, rzeka, bank and ranking between them produce neither, ever, while the velar nasal that bank does produce is one Polish wants. Their records say fricative and retroflex now, written by name through `tools/lang-phonemes.py set`, which reads the field list and the record's length out of the statement rather than assuming either.

No new value rule was needed. The value chain's two arms now call Italian's own fricative rules and the duration chain's call its fricative durations -- four call names -- so the hard series inherits the geminate handling, the amplitudes and the noise modulation a fricative already has. One decision is left for a rule and `pol_sibilant_Fv` is it: `ital_ph_S` and `ital_ph_Z` ask for a place, and it answers the retroflex one when the phone about to be spoken is either hard code and the palatal one otherwise. How a value rule finds its own phone is worth writing down: it loads the left register from the variable at state+832 and scans rightwards over the phone statement, which is what the value chain itself does to decide which rule to call.

Then the letters, and then the spellings, which is where the audible part is: ś as a letter is rare next to si. Polish softens s, z, c, dz and n before an i, and the i is a softener when a vowel follows it and a vowel when one does not -- Kasia is two syllables and dziś is one. `ZZtestFldeq1_4_1` asks whether the next letter is a vowel, and since each test leaves the scan past what it matched, `savescptr` after the z gives a dz span and after the i a dzi span. dzi has to be asked about before dz or dz swallows it.

Two more letters went the same way. Polish g is always a g, where Italian reads gn as the palatal nasal and gi as an affricate, so gniazdo had been coming out with no g in it. And n softens before an i like the rest, giving konie, niebo and nikt their ń. What a rule of ours loses by standing where Italian's stood was worth checking: Italian's n assimilates to the velar nasal before a k or a g and Polish wants that too, and it survives, because the assimilation is in the phonology rather than in the letter.

What comes out now: Kasia takes the soft locus and no hard one, kasza the reverse, cisza one of each, cześć one hard and two soft, szczęście two of each. zima is the soft ź and an i, which is what Polish says and not what it did.

And the numbers are the ear's. Three settings for the hard locus were built and listened to on the same five words -- f2 and f3 at 1700 and 2200, 1600 and 1900, 1800 and 2500 -- and the last was the one that sounded like Polish, which says the first two were too far back for this synthesiser rather than too far back for the language: Italian's own palatal locus is 1800 and 2400 and what won is a hair above it. The build in the tree says those five words byte for byte identically to the clip that was chosen, which is what says the number heard is the number that went in. The soft locus went the same way -- 2000 and 2700, 2100 and 3000, 2200 and 3200, with the hard pair held still so that only one sound moved -- and the highest won there too, which leaves the two series seven hundred hertz apart in the third formant. That is what keeps kasza and Kasia from collapsing back together, and it is a wider gap than the guessed numbers had.

Then the vowels, where the same arithmetic came out again. Italian has seven and Polish six, and the two Italian has spare are exactly the two Polish does not want: the close-mid e and the close-mid o. Polish e is ɛ wherever it stands and Polish o is ɔ, where Italian chooses between each pair by stress and syllable shape, so those two letter rules are one insert each and no tests. o with an acute is u -- mój is muj and sól is sul, where both had an ɔ in them. And j is the glide, where Italian's is an affricate, so mój had been ending in a dż.

Which leaves the close-mid e with nothing producing it, and that is where y went. Polish y is ɨ, a sound no module has, and speaking it as an i costs minimal pairs: być is not bić and syn is not sen. `stanital_ph_e` carries its formants now, which is five numbers in one rule -- the vowel rules are 524 lines of context tests and ten stores, and only five of those stores are the targets. Those five went to the ear the way the sibilants' did: f1, f2 and f3 at 400, 1600 and 2500, at 400, 1400 and 2400, and at 350, 1800 and 2600, on być, bić, syn, sen, ryba, dobry syn and tylko ty. The last won, which puts ɨ nearer i than the textbook would and still keeps być and bić apart -- and the build in the tree says those seven words byte for byte identically to the clip that was chosen.

Taking a vowel over needed one thing the sibilants did not. `make_ital_phon_adjustments` has three arms that put one phoneme in place of another: the first turns an n before a k into the velar nasal, which is why bank is bank and which Polish wants, and the other two close a mid vowel. Those two now put back the vowel they were given, which is both what says Polish has no such alternation and what actually frees the close-mid e -- without it, teraz came out with ɨ in the middle where the letter rule had put an ɛ. Each is IBM's own wrapper with one symbol changed, written as raw lower notation inside a `bare` rule so that nothing else about it can differ, and put back rather than cut out: the same insert of the same length happens at the same point, so nothing downstream can tell that anything was skipped.

So teraz is t ɛ r a z, kolano is k ɔ l a n ɔ, okno is ɔ k n ɔ, dobry is d ɔ b r ɨ, mówię is m u v j ɛ n, and bank still has its velar nasal. The census reads 98% Italian: 1,742 rules of 1,767 character for character, 7 changed and 18 ours alone.

And the letters have their own names, which is what the lone-consonant crash was. A lone letter is spoken by spelling its name out as characters and reading that back as a word, and `ital_char_name` is the dispatch that does it: 205 arms behind a bound test, so Polish's letters at codes 207 to 222 fell off the end of it and the word was left with nothing to say.

Six of them needed a name -- ć, ł, ń, ś, ź and ż, since a lone vowel says the phoneme in its own alphabet record and ą and ę were already speaking -- and 118 of that rule's arms are three lines: the statement type and the count packed into one register, the characters in another, and a jump to the block that inserts them. So six arms of the same three lines went in, the bound went from 204 to 210, and the switch lists 211. cie, eł, eń, eś, ziet and żet, every one of them spelled with letters that already speak, and each spelled name reads back through the letter rules -- the ż inside żet is a letter in a word rather than a lone one, so nothing recurses.

The entries themselves are the dictionary's: six lines in `lang/plpl/plpl.dict` with the action numbers those arms answer to, laid down by `delta-dict.py build` and then dumped back into `plpl.sets` so the text carries them and `make tables-write` cannot lose them.

Doing it that way rather than letting the tool mint the arms was not a preference. `delta-dict.py` can mint one -- it did, and read its own work back -- but the arm it writes names its record by an address in the program, and since the engine stopped needing to be loaded low, an address the arena does not know is a crash. So the tool's write path and the arena scheme have not met, and until they do an arm belongs in the rules text, where `rules/constants` puts the bytes somewhere the arena is told about. That is one of the two things left in the dictionary tooling; the other is that nothing can yet remove an entry, which is what emptying Italian's abbreviations will want.

Then the whole Latin alphabet got Polish names, and Italian's words went.

The two go together and the first forced the second. Twenty-six arms of the same three lines, so a is a rather than ah -- which sounds right today only because h is silent and will stop sounding right the moment /x/ exists -- be, ce, de, ef, gie, ha, jot, ka, el, em, en, pe, ku, er, es, te, faw, wu, iks, igrek and zet. Then c came out as tɕɨ, because the name ce is an Italian function word saying tʃe, and the name was being read as a word rather than as letters. So the letter names cannot be right while Italian's words are in the module.

`lang/plpl/plpl.dict` is the source the tables are laid down from, so removing an entry needs no feature at all: a line dropped from the text is an entry the build does not lay down. Eleven of the thirteen dictionaries are empty now -- 630 entries of Italian function words, roots, abbreviations, tokens, loanwords, clitics and homograph rules -- and what that fixed is more than the names: dom is d ɔ m and not domenica, co is ts ɔ, ok is ɔ k.

One of the twelve cannot be empty and that is worth knowing before emptying anything else. With `prod_prefixes` empty, procent and protokol crash in the durations while produkt and prosty do not, which is the walk-from-nowhere the lone letters used to hit. Keeping its 61 entries costs nothing measurable -- co and coraz still say ts ɔ -- and they are the Latin and Greek prefixes Polish borrowed as well: auto, anti, inter, mono, post, pre, pro, super, tele, trans. So they stay, and the fault they hide is written down here rather than guarded against.

What that leaves audibly wrong in this area: numbers. 123 is centoventitré and 10 is dieci, because the number rules spell a digit into Italian words in the rules rather than in the dictionary, and emptying ital_numbers did not touch them.

And Polish counts. The digits, the teens, the tens, the hundreds, the thousands and the millions, which is everything up to a billion: 1999 is tysiąc dziewięćset dziewięćdziesiąt dziewięć, 2026 is dwa tysiące dwadzieścia sześć, 1234567 is milion dwaset trzydzieści cztery tysiące pięćset sześćdziesiąt siedem.

`it_numbr.dr` is 26 rules over nine thousand lines and covers dates, times, money, Roman numerals, ordinals and fractions as well as counting, but the words are strings a rule hands to a wrapper whose name carries their length, and a word goes in as characters which the letter rules then read -- the same path a letter's name takes. So a Polish word is a constant of ours plus, at the call site, the symbol and the length, and which arm fires for which digit never changes. `pol_insert_<n>` and `pol_insert_l_<n>` in `lang/plpl/rules/it_numbr.up` are the reusable half of that: IBM compiled one insert wrapper per length its own words needed, and these are the lengths Polish's need.

Two things had to be solved rather than typed. Italian writes ventitré as one word and drops the tens' final vowel before uno, where Polish writes two words; rather than touch the rules that join, every tens word carries a trailing space, so the digit after it lands in a word of its own. And Italian's tens reach their insert through a block per length, three words to a block, where Polish's eight tens are eight different lengths, so five of them got a four-line block of their own and the dispatch was not touched at all.

Three faults on the way, all the same lesson: a word's length is stated in more places than the call site. A direct insert has the length and the statement type adjacent on the stack, and changing the wrong one put fifty into statement thirteen. A second arm jumps into the block whose length had been raised, still carrying Italian's shorter word, so 101 read past the end of it and crashed while 102 did not. And an arm takes its word from a register set two hundred lines earlier, so 1001 said tysiąc jede. Each was found by bisecting what had changed rather than by reading it.

What is still Italian in the numbers: two, three and four hundred, which Polish makes irregularly -- dwieście, trzysta, czterysta -- where five hundred through nine hundred are regular and come out right, so 200 says dwaset; the plurals, since tysiąc is tysiące for two to four and tysięcy above, and this always says tysiące; the leading digit of two billion; and ordinals, dates, times, money and Roman numerals, which are each their own rule, so 1. is still primo.

The eight capitals of Polish's own letters were silent, which no test here had caught because every Polish word in them started with a letter Italian has. Żaba said aba, Śnieg said śnieg without the ś, and ŁÓDŹ -- nothing but silent letters -- crashed, the way a word with nothing to say always does. The letter chain dispatches on a character code and never saw them, because `convert_to_lowercase` has no arm for them: it is 62 arms, one per letter it knows, and each arm plants the tag of the next, so adding eight means renumbering the chain rather than appending to a table.

They arrive as their own lowercase instead: eight lines in `lang/plpl/plpl.codepoints`. That is right for every sound and wrong only for anything that would want to know a letter was capital, which nothing in the module does yet. The proper fix is the eight arms, and the reason it was not done that way is written above the lines.

And Polish has its nasal vowels, which no module had.

What made them possible is that the engine already nasalises: `nasalize_vow` in us_filtr.dr nasalises a vowel standing next to a nasal consonant, and it does it by writing **one** global -- a frequency taken from another and raised by 150. So a nasal vowel is a vowel whose own rule writes that global without waiting for a consonant to follow. Two wrong guesses came first and both are worth recording. The parameter the nasal consonants set and the vowels never touch does nothing at all when a vowel sets it -- the audio came back byte for byte identical, and a deliberate sabotage of the same block proved the block runs. And marking a vowel's record as manner "nas" does change the sound, but it makes every rule that asks treat it as a consonant, which sounded nasal and piercing at once.

ę cost no new phoneme code. Every Polish o is the open ɔ, so the close-mid o had nothing producing it, exactly as the close-mid e had nothing producing it before y took it: `stanital_ph_o` now carries ɛ's formants and the nasalisation, and the value and duration chains already had arms for it.

ą had nothing left to take, so it is the first genuinely new phoneme in the module, code 35, added by `tools/lang-phonemes.py add`. Rather than copy five hundred lines of the open o's rule, its arm in `stanital_vals` calls that rule, and two small rules of ours decide the rest: one answers whether the phone about to be spoken is ą, and the other writes the nasalisation if it is. So the plain ɔ and the nasal ɔ share every line of their machinery and differ in one global. kąt and kot are the same phonemes in the same order and different sounds, which is what says it fires.

What that cost is worth knowing before the next new code: a code that has never existed is invisible to every rule that asks what its neighbours are, and what saves it here is that the syllabifier and the stress rules read the phone's *record* rather than its code, and the record says vowel.

And Polish devoices at the end of a word, which is the rule that was making every second word sound foreign: raz is ras, chleb is chlep, róg is ruk, rów is ruf, kod is kot, mąż is mąsz, lekarz is lekasz, wódz is wóts and Łódź is Łuć. Nine consonants do it -- b, d, g, w, z, ż, ź, rz, dz and dź -- and razem, ludzi, woda, dobry and ryba keep their voice, since the rule is about the end of a word and not about the letter.

It also devoices in front of a voiceless consonant, which is the commoner of the two: wtorek is ftorek, wszystko is fszystko, wódka is wutka, kredka is kretka, babka is bapka. That came free -- the same helper answers both questions, so every arm that already called it got the assimilation without another line. What it does not do is carry the devoicing leftwards through a whole cluster: gwóźdź comes out with a voiced ź where Polish devoices that too.

`pol_devoices_here` is the whole of it and it took three wrong tests to write. The scan can always be set, so a scan that fails is not the signal. `advance_tok` succeeds there too. And `testFldeq` answers "no" both when what follows is a letter and when nothing follows at all, so one test of it cannot tell the end of a word from the middle: raz before a space devoiced and raz at the end of everything did not. What works is both halves together -- the scan failing, or what follows being none of the four things a letter's record can say it is. The nine voiceless letters are tested in the same rule, before those four.

Two process notes, because both cost a build each. `make authored` was being run with its output silenced, so when the compiler refused the file -- a place named in one rule and defined in another -- three builds in a row were the same binary and the change looked like it had done nothing. And an edit that lands in the wrong arm of the right rule is invisible until the word that distinguishes them: the word-end test went into the dzi arm rather than the dź one, where it would have turned ludzi into lutsi.

And ch and h say /x/, where ch was a k and h was silent: chleb, herbata, hałas, chcę, mucha, duch.

/x/ is the module's second new phoneme, code 36, and it needed no rule of its own. It is a voiceless fricative, which is what Italian's own S rule makes, differing only in where it is spoken -- so the arm added to `ital_con_vals` hands it to that rule, and `pol_sibilant_Fv` gained a third answer: the velar place, which is Italian's own, the one k and g are spoken at. So the hard sibilant, the soft one and /x/ share every line of their machinery and differ in which locus they ask for. The duration chain needed nothing: /x/ falls through it without a crash, which is worth saying because a new code falling off a chain is what used to crash a lone letter.

What is left after this: Polish's own words, of which the module now has none -- the one-letter prepositions w and z are letter names rather than /v/ and /z/, since a lone letter is spelled before any word dictionary is consulted -- the numbers, the nasal vowels, currently a vowel plus an n, which is what Polish does before a stop and not elsewhere, /x/ for ch and h, which no module has, stress, which is Italian's penultimate rule and near but not the same, and prosody, which is Italian's outright.

And prosody has numbers under it now rather than an opinion. Judging intonation by ear says only that something sounds Italian, which is a statement about two things at once, so `tools/pitch.py` measures instead: it tracks the fundamental frequency of a wav and reduces the contour to what intonation is made of -- where the voice sits, how wide it ranges, how far it drifts down across a phrase, how it starts and how it ends -- with every height in semitones from that speaker's own median, so that a man and a woman with the same melody come out the same. The same tool reads a recording of a person and reads our own output, which is the only reason the two can be held against each other.

Two things it needed before it could be believed. A tracker that decides each frame on its own reads a woman an octave low for whole phrases at a time, because a voice periodic at one interval is just as periodic at twice it; so every frame offers all its candidates, the path is chosen over the utterance at once, and a second pass is offered only the candidates near the median the first pass found. What says that is right is that the melody stops depending on the speaker's sex: over 335 read Polish sentences the men and the women now agree to within a tenth of a semitone on the range, on how far above the middle the voice goes, on how it starts and on how tall its accents are. And Praat, given the same two-pass treatment, agrees with it on all twenty files it was checked against, including the one woman whose voice really does sit at 110 hertz where a fixed floor had put her at 208.

The reference is Google's FLEURS, read prose in a hundred and two languages, of which Polish, US English and Italian were taken -- 338, 394 and 391 recordings by many speakers of each. `NOTICE` says whose it is. None of it is in the tree: what is kept is the numbers it yielded.

What human Polish does, over 335 statements. It ranges four semitones between its tenth and ninetieth percentile. It starts a semitone and a half above its own median and holds that for the first tenth of the phrase. It drifts down about a fifth of a semitone a second from there, ending a semitone below the middle, having fallen about a semitone and a half over the last half second. Its accents are 4.1 a second and 2.3 semitones tall.

Which settles whether to borrow English's melody, and the answer is no. Human English ranges 7.5 semitones where Polish ranges 4.1, its accents are 3.2 semitones against 2.3, it declines twice as steeply and starts twice as high. Polish is the flattest of the three measured -- flatter than Italian, at 4.8. Sounding Italian is not Polish wanting a wider melody.

What ours did instead, and in all three of its languages rather than only Polish: nothing for eighty-five per cent of the phrase and then a cliff. Averaged over 140 sentences and stretched to a common length, the contour sat within a tenth of a semitone of its own median from the quarter mark to the last twentieth, then fell three and a half semitones in that last twentieth -- five semitones over the final three hundred milliseconds, where a Pole falls one and a half over five hundred. Our English and our Italian do the same, so this is the shared machinery and not Italian's tables. And immediately before the plunge there was a rise, a hump of two thirds of a semitone about half a second from the end, which is a late peak of exactly the Italian kind.

Nine numbers in `lang/plpl/rules/is_inton.dr` were what did it, and about seventeen of their units make a semitone, which is measured rather than assumed: the sweep that found them rebuilds in nine seconds with `RULES=bytecode` and speaks all 140 sentences again, so the mapping from a number in a rule to a semitone of audio is a table now. Raising the low targets of `ital_phrase_tone` and of the nuclear falling accent, and lowering that accent's high targets so the hump goes, brings the end of a Polish statement to a semitone and a half below the middle over the last half second: the ramp a Pole makes rather than a step. Questions are untouched, and that is measured too -- `Czy mówisz po polsku?` ends a semitone above its median before the change and after it. The bytecode build and the rules-as-C build give the same numbers to two decimal places.

What is still wrong is the other eighty-five per cent. There is no declination at all: a person descends steadily across a whole phrase and ours is level until the last half second, so a long sentence has no shape. The accents are too few, 2.9 a second against 4.1, and too shallow, 1.75 semitones against 2.3. And the rhythm differs in one thing that can be measured without anybody marking up a recording: the spacing of the syllable nuclei taken from the loudness envelope. Polish syllables are more even than Italian's or English's -- 51 on the pairwise variability index against 57 for both -- where ours scores 56, which is Italian's figure and not Polish's.

And Polish stress is Polish now, which was the largest single thing making the language sound Italian. Every word ending in a consonant was accented on its last syllable, and in Polish that is a great many words: szesnastym came out szesnaSTYM where it is szesNAStym.

What locates the stress is `assign_root_stress`; `assign_primary_stress` does nothing else at all but call it and insert the mark where it left the scan. Italian's locator is a tree of eighty alternatives with a hundred and sixteen more beside it in `antepenult_stress`, which makes seventy string comparisons and is nothing but a list of the words that break the rule, because Italian stress is lexical and has to be listed word by word. And that tree splits on how a word ends. Fourteen words were traced and the split is exact: woda, kolano, herbata, dziewczyna, mucha, ryba and dobry take one path through it and nazywam, szesnastym, dworzec, uniwersytet, lekarz, poniedziałek and kot the other. Italian words end in vowels almost without exception, so the second path is barely exercised there.

`pol_primary_stress` in `lang/plpl/rules/it_strss.up` replaces the pair. It counts the syllables by walking the word at statement type seven, starts again, stops two short of the end and inserts the mark there. Sixteen lines against Italian's thousand and ninety, because Polish has nothing to look up: the penultimate syllable, always.

What proves it is a change of one number rather than an argument. With the walk stopping one short of the end the accent lands on the final syllable, two short the penultimate, three short the antepenultimate -- moving exactly one syllable each time, and identically for a word ending in a vowel and one ending in a consonant, which was the whole of the bug. What makes that readable is nonsense words of two to five syllables separated only by /t/, since every voiced stretch is then exactly one syllable and the accent's position can be counted off. Italian's locator on the same words puts the accent on the third syllable of a five-syllable word where Polish wants the fourth.

Two things learned on the way are worth more than the fix. A rule cannot answer through `get_parm_ptr`: it copies the caller's value into a local of the callee's and pushes a pointer to that local, so a `savescptr` into it writes nothing the caller will ever read. Four separate experiments appeared to be moving the stress and were all of them no-ops, and what caught it was holding them against a rule whose entire body was `match`. The general form of that check is worth keeping: when a change to a rule seems to work, compare it against a rule that does nothing, not only against the rule it replaced.

And prominence in this engine is pitch and not length. In szesnastym the syllable stym runs a quarter of a second where na runs a seventh of one, which reads as stress and is not: stym has four phonemes and na has two. With the stress mark removed altogether the lengths do not move at all and the pitch goes flat. A duration is not evidence about stress, and an amplitude envelope is no use either -- a formant synthesiser barely dips between syllables, so the syllable detector that works on a corpus of people found one syllable in nazywam.

By ear it is still not right, which the measurements do not say and are not able to. Three things are known to be left, in the order they are worth trying. The syllabifier is still Italian's, so a rule that correctly asks for the penultimate syllable can still be handed the wrong count: `create_ital_syllables` knows nothing of Polish consonant clusters, and szesnastym, poniedziałek and dziewczyna are exactly the shapes where it would differ. Secondary stress was not touched at all -- `assign_secondary_stress` still calls Italian's locator, with Italian's lexical exceptions inside it. And the accents are still too few and too shallow against the corpus, 2.9 a second at 1.75 semitones where a Pole has 4.1 at 2.3, with no declination across the phrase at all.

The end of a Polish statement was also taken a semitone deeper than the corpus figure, by ear. Matching a person's terminal fall exactly left it sounding unfinished, which is a limit on the whole method worth stating: the numbers say where to aim and what has moved, and they do not say when it is right.

## Catalan

`lang/caes` exists and speaks Central Catalan. As it stands it is Castilian Spanish under a Catalan name -- made by copying `lang/eses`'s five text forms and its rules and renaming them -- and `NOTICE` says the licence consequence, which is that all of it is IBM's Spanish data until it has been replaced. It is family eighteen, `0x120000`, the last of the eighteen the three family-indexed tables hold: `src/eci_voicetable.c`, `src/eci_dict.c` and `src/eci_romanizer.c` all count families from one to eighteen, IBM used six, four more have a romanizer its own code insists on, and Polish took seventeen. A tenth language will have to widen those three.

Spanish is the template for reasons rather than convenience, and the reasons run the opposite way from Polish's. Its consonants are very nearly Catalan's: it declares the voiceless and voiced postalveolar fricatives, both postalveolar affricates, the voiced s, the palatal nasal and the palatal lateral, the tap and the trill, and merely never reaches six of them from Castilian spelling. Its vowels are the worst of the nine: five, no tenseness field, and no reduction at all, where Catalan has eight and reduces three of them away in every unstressed syllable.

### It was built without IBM's objects

The recipe in `docs/building.md` begins by writing the template's rules out as text, and that is the one step that opens an object. Spanish's rules as text were not in the tree and the SDK was not either. So they were read back out of the bytecode that is in the tree, by `tools/delta-unemit.py`, which inverts the emitter operation for operation. What says that is exact is not an argument about the encoding: written out, `delta-notation.py regenerate` emits the whole module again from that text and it comes back byte for byte -- all three files, 1,724 rules, 744,858 bytes of bytecode, no object opened.

### The vowels

Catalan has eight vowels to Castilian's five, and three of them had to be made: the schwa (code 37), the open e (38) and the open o (39). `cat_ph_schwa_vals`, `cat_ph_eopen_vals` and `cat_ph_oopen_vals` in `lang/caes/rules/ss_ssval.up` are five formant targets each, and the open pair carries a sixth number for the reason two paragraphs below.

Reaching them meant writing `stanspan_vals` again. That rule is the vowel value chain -- seven alternatives, each testing the phone against one vowel and handing it to that vowel's rule -- and a code with no arm falls past every one of them into the tail with no formants set at all, so the vowel comes out as whatever the phone before it left behind. The rewrite has three arms more, and the reduction in three of the others: the a, e and o arms ask the phone whether it carries stress, which the phone statement records at offset 8 and which nothing in Spanish has ever had a reason to read, and hand an unstressed one to a different rule. Every unstressed a and e is a schwa and every unstressed o a u, which is what Central Catalan does and what nothing in Spanish does.

The reduction is there rather than in the letter rules because when a letter is read the syllables have not been counted and the accent has not been placed; by the time the value chain runs, both have.

**The value chain is not the only chain that asks which vowel a phone is.** Writing `stanspan_vals` again gets the formants right and stops there, and for a long while that looked like the whole of it, because the schwa is not a phone code: nothing lays code 37 down, the reduction happens inside the value chain on the a or the e the letter rules already wrote, and every rule downstream still sees a vowel Spanish knows. The open pair are real codes, laid down by `apply_span_enye_rules`, and they are invisible to every other chain that dispatches on one. `get_stanspan_nuc_AV` is the one that matters: it sets the amplitude of voicing, at offset 2722, out of an arm apiece for i, e, a, o and u, and a code with no arm leaves the field alone -- so the open vowels were spoken at whatever amplitude the phone before them had left there. After a d or a p that is silence. cafe with a grave was `k @ f E` in the phonemes and a 300-millisecond hole in the audio, and of the eight words in `out/catalan/13-obertes.wav` five had one; `pitch.py` put the whole file at 58% voiced with the syllables 264 milliseconds apart. It was reported as the vowels failing and as the intonation failing, and it was one fault.

The fix is a sixth number in each of the two rules, and it is sound rather than a race because the chain that would overwrite it has nothing to say for these two codes: `get_stanspan_nuc_AV` runs after them, from inside `insert_acoustic_vals`, and leaves 2722 as it found it. IBM's numbers equalise rather than follow sonority -- 55 for i, 50 for a, 53 by default, a high vowel radiating less and being given more -- so 55 and 54 are what put the open pair inside the band its own five occupy, measured over tapa, tepa, tipa, topa and tupa against tepa and topa with a grave. The file is now 76% voiced with the syllables 176 milliseconds apart, and of the twenty-three cases only the two with a grave in them changed a byte.

The same trap was in the same shape one rule over, and that one is fixed too. `assign_cast_start_dur` is how long a vowel is held -- an arm apiece for a (120), e (110), i (100), o (110) and u, whose arm tests and discards its answer and falls into the 100 -- and anything else that reaches it falls there as well, so the open pair were given the duration of the shortest vowels in the language. The amplitude could be set from the value chain because the chain that would overwrite it says nothing; this one cannot, because the duration is assigned before the value chain runs. So the rule is written again in `lang/caes/rules/ss_ssdur.up` with two arms more.

Its three numbers turn out to be a straight line in the first formant, exactly: i and u near 300 hertz take 100, e and o at 480 take 110, a at 660 takes 120, which is ten for every hundred and eighty hertz with no residual at any of the three. So the scale is openness and it says where the open pair belong without a judgement of ours -- 600 and 580 hertz both give 116, and they take one number because they are the same height as each other. Over the frame tapa/tepa/tipa/topa/tupa the word durations now climb with the first formant as IBM's own scale does, 0.690 to 0.711, with the open pair at 0.705 where they used to sit at 0.690 with the shortest.

What that rewrite is held to is what `make upper-check` holds English's to, done by hand because the harness cannot do it here: `upper-check.sh` compiles every `.up` file in a language at once, and for Catalan the side without them is not IBM's Spanish rule in our module, it is Spanish. So the comparison was made against a build of this tree with only `ss_ssdur.up` taken out. Over a sentence with no open vowel in it the two traces are identical, 175,483 lines, call for call. Over one with both, they are 18 lines apart in 75,408 with the rule numbers masked the way `upper-check` masks them, and all 18 are the two new arms firing -- nothing of IBM's is missing. And over the twenty-three cases only the two with a grave in them changed a byte.

The consonant locus rules `span_pal_Fv` and `span_trill_Fv` have the same shape and the same gap, and are not fixed. What they cost is a transition rather than a segment.

**The two accents.** Catalan writes the acute for a close vowel and the grave for an open one, and both mark the stress; Castilian writes only the acute. `normalize_span_letters` flattens every character Castilian does not use, so a grave lost its accent before the stress was assigned and the reduction then ate the vowel the accent was there to protect. The two arms that flatten e and o with a grave are reached through one wrapper apiece, used nowhere else, and a wrapper that never matches is that arm taken out of the chain. The letter then has to go somewhere, and the letter chain has no arm for it -- so it takes the one Castilian keeps for n with a tilde, which Catalan never writes, since it spells that sound ny. `apply_span_enye_rules` asks which grave it is and lays down the open vowel. So dona with a grave and dona with an acute are a minimal pair again, `d O n @` against `d o n @`. a, i and u with a grave are not open vowels and arrive as their acutes, in `caes.codepoints`.

**i and u are vowels, not glides.** Spanish makes either into a glide beside another vowel, so dia was one syllable, `d y a`; Catalan has hiatus and says `d i @`. Both letters now take the glide only after a vowel -- caixa, peix, noia, taula, dijous -- and u also after a q or a g, for quatre and aigua. Getting one right without the other is worse than getting neither: with only i changed, ui came out as two glides with nothing between them and vuit crashed in the durations with no nucleus to shape.

The three vowel letters gave up their diphthongs to make that work. Castilian's a, e and o rules are long because they are where its diphthongs are decided -- an a followed by an i eats the i and lays the glide down itself -- and Catalan decides the same thing from the other end. Having both is why ix could not be fixed: the i of caixa never reached its own rule to eat the x, because the a had already taken it. The three lay down their vowel now and take nothing with them.

**And ix keeps its glide.** For a while it did not: the i was read as a graphic mark saying only that the x is not k and s, so caixa was `k a S @` and calaix `k @ l a S`, which a listener heard as calash. The i is a sound as well. Where a vowel comes first the letter says a glide and then the fricative -- caixa `k a y S @`, peix `p e y S`, calaix `k @ l a y S`, llegeix `L @ Z e y S`, això `@ y S O` -- and where none does it is the fricative alone, which is the arm that was there. Central Catalan is the dialect that drops the glide and Western and Valencian keep it, so both are Catalan and this is the one that can be heard. It is two phones from one letter, which `insert_2pt_s`'s `_4_2` form already does for the affricates, so `rules/constants` holds the pair as `cat_ph_ysh` and nothing new is asked of the module.

**And the postalveolar is not held for a sixth of a second.** Castilian declares S and no Castilian spelling reaches it, so the number IBM left in `span_ph_S_dur` had never once been spoken: 150, flat, for every S not standing after a t. Catalan reaches it in x, ix, caixa, xocolata and això, and 150 is not a fricative. Measured on our own output it was 145 milliseconds against 65 for the s of passa -- the sound the language writes most often was more than twice the length of the one beside it. It is 80 now, which is longer than the s as a postalveolar is in every language that has both and not double it, and the one context IBM did write is kept: the S of an affricate, tx of cotxe and despatx, still takes 40. `ss_dur.up` has the rule; what it leaves out is IBM's scan loop for skipping synchronisation marks on the way left to that t, and the audio says it is not needed, since cotxe still measures 50 milliseconds and despatx 55, exactly what they were.

**Two vowels that should stay apart.** `syneresis` is Castilian's pass for running two vowels into one syllable, and it undid what the letter rules had got right: duana was `d w a n @`, suau `s w a w`, serie `s E r y @`, each a syllable short. Catalan says the two, and the words where it does not are a dictionary's business. The rule is replaced by one that lays nothing down.

### The consonants

j, and g before e and i, are the voiced postalveolar where Spanish has a velar fricative -- a fricative on its own and an affricate after a nasal or a lateral, which is the paragraph below. x is the voiceless postalveolar wherever it stands. z is a voiced s and c with a cedilla a voiceless one. c before e and i is a plain s. ny is the palatal nasal. tx is the voiceless affricate, and tg, tj and tz voiced ones. s between two vowels is voiced where written double it is not, which is the whole reason Catalan spells it twice.

**j and g before e and i harden after a nasal or a lateral.** Catalan says that sound as a fricative on its own -- jove is `Z O B @`, gener `Z @ n e`, girafa `Z i r a f @` -- and as the affricate directly after an n or an l: penjat is `p @ N d Z a t`, menjar `m @ N d Z a`, mengen `m e N d Z @ n`, angel `a N d Z @ l`, monja `m o N d Z @`, injust `i N d Z u s t`. It is the same affricate tg and tj already spell in metge and platja, so it lays down the bytes `rules/constants` already held for those and asks nothing new of the module.

The n in front of it is the palatal nasal rather than the alveolar one, which is the assimilation Castilian already makes before its own ch and which Catalan spelling reaches through two more letters. Asking that of the letters means asking about two of them, since ge and gi harden and gue, gui and ga do not: distingir is `d i s t i N d Z i` where distingueix is `d i s t i ng g e S`, and sang, mango and llengua keep the velar the phrase-level rules give them.

What it cannot see is the word before, because a letter rule is handed the run it is reading and the scan stops at the word boundary. So el jove keeps the fricative where a speaker would harden it, and that is a phrase-level rule rather than a letter one.

**ll is a lateral.** Spanish declares the phone L as a voiceless palatal *affricate* -- Castilian is yeista and its ll is not a lateral at all -- so every rule that asked what kind of sound it is got the wrong answer. Its record now says voiced, sonorant, liquid, palatal.

Its record was not the whole of it. `span_phrase_level_rules` has an arm that rewrites L into y where the word before ends in a sonorant, which is Castilian's approximant and is right for Castilian; in Catalan it fired at nearly every word boundary the language writes an ll after, so la llum was `l @ y u m` and el meu llibre `m e w y i B r @` while llibre alone was right. The arm is reached through one wrapper, `ZZtest_synch20_ZZstring77`, and through no other call in the module, so a wrapper of ours that never matches takes it out of the chain -- the same move that freed the two graves, and the reason to count a wrapper's callers before rewriting one.

**An n at the end of a word stays alveolar.** Castilian velarises it -- gran was `g r a ng` -- and the change is made in `span_phrase_level_rules`, 588 lines that do a great deal else besides. But it is made on the phone's code, so a word-final n that is some other code goes through untouched. T is that other code: Castilian spells it with c before e and i and with z, Catalan says a plain s for both, and no Catalan spelling can reach it. Its record now says what n's says and `span_ph_T` forwards to `span_ph_n`, so it is an n in every way the engine can ask about. What that keeps is the assimilation that is right -- banc is `b a ng k`, canvi `k a m b i`, enfadat `@ m f @ D a t` -- while gran, tren, bon, un and mon end in the alveolar. Retaking a code the language cannot reach is what the notes on adding a phoneme advise over adding one that has never existed, and this is the third time it has paid here.

It had a price, and it went unnoticed for a while because nothing in the phoneme output shows it. `assign_span_stress` decides between the last syllable and the one before it by asking what the last phone is -- a vowel, or an n or an s with a vowel in front of it, takes the penultimate -- and it asks about the n by its code. Catalan's word-final n is not that code, so every word ending in one fell through to the last syllable: canten was `k @ n t e n` where Catalan says `k a n t @ n`, and with it every third person plural there is. The test cannot be added beside the other two, because the wrapper that carries it is the same one the velarising arm reads and widening it would undo what the retaking was for. So it is caught at the other end instead, in `lang/caes/rules/st_strss.up`: `span_final_str` is nine operations long, and a word reaching it that ends in the final n with a vowel before it is handed to `span_penult_str`. govern, which ends in an r and an n, still takes the last syllable.

**A stop devoices at the end of a word and before a voiceless one**, which Castilian does neither of: fred is `f r e t`, verd `b e r t`, club `k l u p`, dissabte `d i s a p t @`, dubte `d u p t @`. What says the word has ended is two things together, because neither alone will do: the scan fails, or what it lands on is not a letter. The five voiceless letters are asked about once, in `cat_voiceless_follows`, rather than five times in each of the three stop rules.

**qu is k before e and i, and Catalan writes a fifth letter there.** Castilian's rule asks about e, i and the two of them with an acute, and eats the u; que` and perque` carry the grave, which `caes.codepoints` deliberately does not flatten because it is the open vowel, so the u was read as the glide it is in quatre and perque` came out `p @ r k w E`. The grave is asked about now with the four.

**A word-final r is not said** where the word has a second vowel in it: passar is `p @ s a`, cagar `k @ G a`, carrer `k @ R e`, amor `@ m o`, parlar `p @ r l a`, and carrers and dinars with them, since the plural is added to a word that had already lost its r. clar, cor, mar and rar keep theirs, and so does the trill at the start of a word and after l, n or s. The condition is not the ending but the word, which is why it is asked of the letters: the scan is set leftward from the r, the vowel before it stepped over, and the letters behind that walked until a second vowel turns up or the word runs out. `advance_tok` is the step, `setscan_l` sets the direction, and the walk is written out eight times rather than looped, because the notation's `while` is a backtracking loop and wants a choice point that a bare rule has no business planting.

Dropping it outright does not work, and what happens instead is the whole of why this took so long. `assign_span_stress` decides between the last syllable and the one before it by looking at the last phone, so a word left ending in a vowel takes the stress a syllable back: passar came out `p a s @`, which is a different word. So the r becomes a phone rather than nothing -- the velar fricative, code 16, which Catalan cannot reach because j and g before e and i are the voiced postalveolar here. Its record now says undefined manner and undefined place so that no neighbour coarticulates towards a velar, `span_ph_j` lays nothing down at all, and `span_ph_j_dur` gives it twenty milliseconds, which is the compensatory lengthening a lost r leaves behind. The word still ends in a consonant when the stress is placed, and says nothing when it is spoken. That is the fourth time retaking an unreachable code has paid here, and the first time one has been retaken to be silent.

**A final t after an n or an l is not said.** gent is `Z e n`, sant `s a n`, vint `b i n`, molt `m o l`, content `k u n t e n`, and every adverb in -ment with them; tot, fet and gat keep theirs, having no nasal or lateral in front. It is done the way a final r is done, in `apply_span_t_rules`, and for the same reason: a word left ending in an n would have taken the stress rule for a word ending in a nasal and moved its accent a syllable back.

**The Castilian words are gone.** 600 entries of function words, abbreviations, tokens, loanword spellings and verb forms were dropped from five of the six dictionaries -- a line taken out of `caes.dict` is an entry the build does not lay down. The sixth is `span_char_name`, the names a lone letter is spelled with, kept because a letter with no name has nothing to say and crashes.

### The words that carry no stress

Dropping Castilian's function words left Catalan's unstressed words stressed, which is what a listener hears first: del tot came out `d e l t o t` rather than `d @ l t o t`, a casa `a k a z @`, per la casa `p e r l @ k a z @`. Where those words live took some finding, because it is not the dictionary. `span_funct_words` looks a token up in `span_funct_dict` and then, when that fails, tries ten **lookup sets** in turn -- the determiners, three lengths of preposition, the two kinds of conjunction, the object and possessive pronouns, the relatives and the demonstratives -- and a word found in any of them is marked a function word rather than a content word. A content word is given a stress by `find_most_stressed_syll` and a function word is not.

The sets are in `caes.sets` as bytes: one code per character of the language's own alphabet, a nul after each word, sorted by those codes because the engine binary-searches them. `tools/lang-sets.py` is what reads and writes them by name, the way `lang-alphabet.py` does a letter's record and `lang-phonemes.py` a phone's; `lang-sets.py check` says every set is well formed and in the order the search needs, over all nine languages, and it found one of IBM's own French-Canadian sets out of order while it was at it.

The ten now hold Catalan: a, al, als, amb, de, del, dels, en, per, pel, pels; the articles el, els, la, les and un, una, uns, unes; the weak pronouns em, et, es, el, els, la, les, li, ens, us, hi, ho, en, me, te, se, ne, nos, vos; i, o, ni, però, sinó; si, perquè, mentre, encara; and the prepositions, relatives and demonstratives with them. Only a word of one syllable is destressed by being there, which is what makes the list safe to be generous with: entre, aquest and mateix are function words and keep their accent, as they should.

Two things it taught. A monosyllable in one of those sets is destressed whether or not it should be, so the tonic ones have to stay out: cap, fins, dins, vers and quan are prepositions and conjunctions that carry stress, and putting them in would have flattened them.

That lesson was written down and then not applied to the set next to it. The relatives were left as IBM's list of them -- on, com, qual, quals, quan, quant, quanta, quantes, quants, que, qui, quin, quina, quines, quins, què -- and every monosyllable in it but one is tonic in Catalan. Being there destressed them, and an unstressed o is a u, so com was `k u m` and on was `u n`: Com estàs came out as cum and On vas as un vas, a different word. quan lost its a to the schwa the same way. The set now holds que, which really is the atonic one, and the multisyllables that are harmless because only a monosyllable is destressed by being there. Com estàs is `k o m @ s t a s`, On vas is `o n b a s`, Quan vindràs is `k w a n b i n d r a s`, and Que plou is still `k @ p l o w`. It was found by ear, and what it says about the method is that a list copied whole from the template is not checked by anything: the census counts a set that has been rewritten, not one that should have been. And Castilian's possessives mi, su and tu are in `shortposs_pros` because they are proclitic, where Catalan's meu, teu and seu are not -- with them in, el meu llibre came out `m @ w`, and the set now holds the handful of Catalan possessives that really are atonic: ma, mon, mos, sos, ta, ton, tos.

### What it says

    casa       k a z @        Barcelona  b @ r s @ l o n @
    dia        d i @          policia    p u l i s i @
    cafe`      k @ f E        dona`      d O n @
    dona'      d o n @        opera`     O p @ r @
    caixa      k a S @        peix       p e S
    dijous     d i Z o w s    vuit       b u y t
    duana      d u a n @      serie`     s E r i @
    fred       f r e t        llibre     L i B r @
    metge      m e d Z @      xocolata   S u k u l a t @
    passar     p @ s a        carrer     k @ R e
    clar       k l a r        cor        k o r
    canten     k a n t @ n    gent       Z e n
    la llum    l @ L u m      del tot    d @ l t o t
    penjat     p @ N d Z a t  mengen     m e N d Z @ n
    jove       Z o B @        distingir  d i s t i N d Z i

`make EVVLANG=lang/caes TEMPLATE=eses census` counts what is left: 96% of the rules are still Spanish character for character -- 1,685 of 1,746, with 38 changed and 23 of ours alone -- and of the tables, 113 lines of the sets are ours with 307 of Spanish's gone, 7 lines of the statements, 4 of the constants and 2 of the settings. It wants Spanish's rules as text, which are not in the tree; `python3 tools/delta-unemit.py eses` writes them and they can be thrown away again afterwards.

**The schwa is set at 600, 1500 and 2450**, and that number came from a report rather than a measurement. At 500 it was heard as far too close -- "almost like an o" -- so it was opened by a hundred hertz in the first formant, which is the direction that complaint points in and about the distance between one vowel height and the next. Four other settings were built on the same six words and are in `out/catalan/schwa`, from 500 to 660, so it can be revisited without rebuilding anything. It is the one number in the module that no measurement here can settle: `pitch.py` says nothing about vowel quality, and there is no Catalan recording to hold a formant against.

### What is not Catalan yet

**Unaccented stressed e and o are the close pair**, always. Where the writing has no accent Catalan still has both vowels -- sec and sec are spelled the same -- and telling them apart is a dictionary's job, which this module has none of.

**The monosyllabic infinitives keep their r.** dir, fer and ser are [ˈdi], [ˈfe] and [ˈse] and come out `d i r`, `f e r` and `s e r`, because the rule that drops a final r asks for a second vowel and they have not got one. That test is what keeps clar, cor, mar and por, so it is right about far more words than it is wrong about; the handful it is wrong about are a dictionary's business, and there is no dictionary.


**The numbers count to ninety-nine in Catalan.** Where they live took some finding. A digit is not spoken by the number rules at all: it is a word in `span_char_name`, the one dictionary that was kept, so 5 said cinco because that dictionary said so. Putting a Catalan word there does not work, and the reason is not the one the tooling reports: the record wants a length the rule already lays down, and padding to a length it does lay down gets past that check and the digit comes out silent anyway, as does a rewrite that keeps the original length exactly. Anything `delta-dict.py` writes is reached by an address the arena does not know, and that is as true of a record it rewrites as of an arm it mints. `docs/building.md` records the gap; what is new is that it is wider than it said.

So the numbers are said by rules, out of bytes in `rules/constants`, which is a store startup copies into the arena. `lang/caes/rules/st_numbr.up` holds three written afresh -- `convert_span_single_digit` for one to nine, `convert_span_teens` for ten to nineteen, `convert_span_twenties` for the rest of the twenties -- each ten arms testing a digit and laying a word down whole. The tens from thirty to ninety needed no new rule at all: `convert_span_double` pushes a symbol and inserts a fixed number of characters from it, so pointing seven of those pushes at bytes of ours puts Catalan words in Castilian's place. Two call sites were widened by a character, because seixanta and vuitanta are one longer than sesenta and ochenta, and three words were padded to match the arm they share.

Which digit an arm is looking at is not something to reason about. The three new rules read from different places: the digits scan rightwards from the left-hand end, the teens leftwards from the right-hand end, the twenties rightwards from the right-hand end. Each was found by trying all four and seeing which told the ten words apart; the other three made every number in the range say the same thing. The same is true of which insert an arm jumps back to -- 80 was truncated to vuitant until the right one of nine was found, and only trying said which.

Catalan runs a compound number together into one word where Castilian writes three -- vinticinc, quarantacinc, not vint i cinc, cuarenta y cinco. `convert_span_double` puts the space and the y in between with two calls, and those two call sites now lay down the mark alone and nothing, so the tens word and the unit meet.

Joining them made two rules fire across the join that should not, and both were answered by spelling the word for the sound rather than for the page. An s between two vowels is voiced in Catalan and here it must not be, so six and seven are written with two esses -- seixantassis, not seixantasis -- which is voiceless and reads back the same when the digit stands alone. And an i before a u makes the u a glide, so one is written hun, an h saying nothing: vintiun is [b i n t i u n] and not [b i n t i w n].

What is left: a bare 0, which never reaches these rules because `single_chars` spells it as a character, and the hundreds and thousands, which are Castilian entire.

**The letter names** are Castilian, for the dictionary reason above. The way past it is the one the numbers took: say them from a rule. **The syllabifier and the stress rule** are Spanish's, and their defaults are Catalan's often enough to hide the cases where they are not. **The rest of the prosody** is Spanish's. What has been done to it is what could be done without a corpus, and it is measured; the section below is that.

### The melody

`tools/pitch.py` reads our own output, so the engine can be held against the human figures already in this document even where there is no Catalan recording to compare with. What was missing beside it was a set to measure over and a description of what Catalan is supposed to do. Both are here now.

**What Catalan does**, out of Prieto's *The Intonational Phonology of Catalan* and the Cat_ToBI proposal behind it. A broad focus statement is rising prenuclear accents with the peak on the syllable *after* the accented one -- L+>H*, which is what makes the lilt -- downstepping across the phrase, and a nuclear L* with extreme downstep and final lowering to L%. At the right edge of an intermediate phrase, a comma, the voice goes *up* rather than down: H%, the continuation rise, or a sustained pitch. A wh-question is a high tone on the wh-word and a descending pattern from there to a nuclear H* or a downstepped !H+L*, then L%, which is to say it ends like a statement. A polar question has two neutral shapes in Central Catalan and both are unlike Spanish: the commoner one *falls*, held on a high plateau through the whole prenuclear stretch and then H+L* L%, which Prieto calls out as running against the crosslinguistic grain; the other rises, and its rise is sharp -- L* HH%, a low nucleus and then a jump to near the top of the speaker's range, not a drift.

**What to measure it with.** `out/catalan/prosody.txt` is twenty-four sentences under four headings -- twelve statements, four polar questions, four wh-questions, four with a comma in them -- and `tools/melody.py` speaks all of them and prints `pitch.py`'s numbers averaged by heading, with `-o` to keep a set and `-c` to print the change since one. That the average is the thing to read and not any one sentence is not a nicety: the terminal fall of a single utterance depends on what its last word happens to end in, and one ending in a reduced vowel moves it by several semitones. `melody.py` adds one number `pitch.py` has not got, `before_pause`, which is where the voice is left over the eighty milliseconds before a silence in the middle of an utterance -- because the last half second of a sentence with a comma in it is still the full stop, and nothing else here could see what a comma does.

**Four numbers in `ss_inton.dr`**, and the pair of them together is the point rather than either alone. The low targets of `span_nucl_low_tone` go from 90, 90 and 88 to 58, 58 and 56, and the phrase-final target in `span_phrase_tone` goes from nought to 92. That second one had not been found before. The rule holds five branches that write a phrase-final target and a statement takes none of them: the arm it does take stores a register that is nought, so the value is the nought the rule initialised, and every statement fell to the floor of the F0 window. Raising the nuclear lows on their own, which is what was done before, took the accent before the fall higher and left the fall to nought, so the two numbers pulled against each other; moving both puts the last accent lower and the end of the phrase off the floor.

Over the twelve statements, against a person as measured over FLEURS:

    tail          -1.94  ->  -0.87    a person is at -1.0
    terminal fall -5.95  ->  -3.66    a person falls 1.5
    range          5.38  ->   4.96    a person ranges 4 to 4.8
    onset         -0.41  ->  +0.02    a person starts at +1.5
    accents/s      3.07  ->   3.44    a person has 4.1

Five moved toward the human figure. Three moved slightly away and are worth naming rather than hiding: the declination steepened from 1.35 to 1.54 semitones a second where a Pole drifts down a fifth of one, the accents grew from 3.08 semitones to 3.21 where a person has 2.3, and the rhythm's variability fell from 50.1 to 48.6 where a person is at 51. The first is the least uncomfortable of the three, since Catalan is a downstepping language and the declination figure in this document is Polish's; the other two are small and were not what the change was aimed at.

**The comma rises now**, which is the piece of Catalan the numbers had never been asked about. `before_pause` goes from -0.23 to +1.04: the voice is left a semitone above its own median at an internal phrase edge where it used to be left at the middle, and a full stop still ends below it. That is the continuation rise, and it came out of the same four numbers rather than out of a change of its own.

**A polar question rises harder**, from a tail of 1.63 semitones above the median to 3.96, with the nucleus before it lower -- which is the low-then-sharp-rise Cat_ToBI writes L* HH% rather than the drift it was. That was not aimed at either: lowering the nuclear accent lowers the last accent of a question as much as of a statement, the boundary a question ends on is untouched, so the rise between them got bigger. It happens to be the right direction, which is worth saying plainly because it was luck and not design. The falling polar question, which is the commoner Catalan shape, is not attempted: it wants a high plateau over the prenuclear stretch to tell it from a statement, and nothing here yet knows how to hold one.

It is deliberately not the human figure. The Polish work found that matching a person's terminal fall exactly left the speech sounding unfinished and took it a semitone deeper on purpose; this is deeper again for the same reason. That is the limit of the method and it is worth restating: the numbers say what has moved and which way, and they do not say when it is right.

**And on that occasion they did not.** A listener said the end of a statement sounded high -- not a question, not an exclamation, just a thought being finished on the wrong note -- and the numbers had said the opposite: a tail of -0.87 against a person's -1.0 is as close to the human figure as this document has got anywhere. What the numbers could not see is the *shape*, and `pitch.py trace` shows it in one look. Over the last two hundred milliseconds of *Barcelona és la capital de Catalunya* the voice climbed from 107 to 115.7 hertz -- above its own median -- and only then fell. The peak was landing on the final unstressed syllable. An average tail cannot tell a fall from a rise-then-fall that ends in the same place, and this was the second.

The phrase-final target in `span_phrase_tone` goes from 92 to 50. The climb is gone -- the same sentence now sits flat at 102 and falls away to 86 -- and the tail goes to -2.29 with the terminal fall at -5.65. One number moved away from the human figure and is worth naming rather than hiding: the range widens from 4.96 semitones to 5.53 where a person is at 4.1, which is what a deeper fall out of the same accents has to do. Three things are worth keeping from it. Raising that number from nought was right and raising it to 92 was too far, so the fault was the size of a correction rather than its direction. The number moves statements and touches nothing else: the polar question's tail stays at 3.96 and the comma's continuation rise at +1.61, both to the hundredth, because the branches they take are other arms of the same rule. And an average over twenty-four sentences is the right tool for asking whether a change moved the melody and the wrong one for asking what the melody is; the contour has to be looked at.

**A lone i is spelled rather than said, and it is the commonest word in the language.** Catalan's coordinating conjunction is i, and it is in `coord_conjs_setentries` with o, ni, però and sinó -- but it is never found there, so `find_most_stressed_syll` gives it a stress and *pa i vi* has three stressed words where it should have two. The cause is upstream of the sets and was found by tracing a sentence against the same sentence with o in place of i. `single_chars` in the shared normaliser hands a lone character to `char_name` and so to `span_char_name`, the letter-name dictionary and the one of the six the 600 Castilian words were not dropped from. It matches i, calls `insert_space`, and the token that reaches `span_funct_words` a few thousand trace lines later is no longer the word: all ten set lookups answer no. It does not match o, or a, because those are words in Castilian and i is not.

Three things were tried and none of them is the answer. Taking the i and I entries out of `caes.dict` and running `delta-dict.py build` changes nothing -- the arm still fires. Putting i into a second set, in the middle of `short_preps` where its neighbours are found, changes nothing either, so it is not the binary search and not the set. And there is no single-use wrapper to take out of the chain the way the two graves and the ll were freed: `span_char_name` reaches `insert_space` from one call site shared by every letter. What it wants is the arm that spells i taken out for this language, and where that switch lives -- why the dictionary holds o and the lookup does not find it -- is not yet known. Until then i is a stressed content word, which is audible in any sentence that lists two things.

**A wh-question still rises, and it should fall.** This is the one piece of the research that could not be applied, and what stands in the way is not a number. `question_phrase` asks `isitWH`, which asks `isit_span_WH`, which tests the first token's `subcat` field against `interrog` -- and no Catalan word ever carries it. That field is written by `word_category_dictionary`, which is a lookup in `span_funct_dict` dispatching on the action it finds into eighty-seven arms, and `span_funct_dict` is one of the five dictionaries the 600 Castilian words were dropped out of. So `assign_wh_quest_features` is never entered, every question takes `assign_default_quest_features` instead, and the interrogatives end on the polar question's boundary tone. Traced: `isit_span_WH` runs seven times on a Catalan wh-question and answers no every time, and it answers no to the Castilian spellings as well, so it is the emptied dictionary and not the accents Catalan writes differently.

What that costs is worth stating exactly, because it is more than the tune. Every one of the eighty-seven arms of `word_category_dictionary` is a category and a subcategory that no Catalan word now gets, and the intonation is the part of it that can be heard in a short sentence. Putting the interrogatives back means entries in `span_funct_dict` pointing at arms that already exist -- an entry with nothing after its action lays no record down and only marks fields -- in the character order the engine binary-searches; and then the branch of `span_phrase_tone` a wh-question lands on has to be found the way the polar one was, by changing a number and seeing which sentences move.

What is untouched is everything before the last half second, apart from the phrase-initial F0, which was measured and left alone: raising `cast_phr_initial_F0`'s 70 to 95 moved the onset by six hundredths of a semitone, so whatever decides where a Catalan phrase starts, it is not that branch. There is still no Catalan corpus here -- FLEURS has one and it has not been fetched -- so the range, the declination and the rhythm are held against Polish's figures for want of Catalan's own.

The rules here were shown to be the code that runs rather than assumed to be. `apply_span_x_rules` was made to say k where it says the postalveolar, and the audio moved; put back, it moved back. What that also showed is worth keeping: caixa did not change under the sabotage, because its postalveolar comes from the i rule eating the x rather than from the x rule at all, which is exactly the sort of thing a sabotage is for.

There is no oracle -- IBM never made a Catalan module -- so the ear is the judge, and everything in this section was found by one. That is not a remark about thoroughness. The glides had been swapped in the rewritten vowel chain, every check in the tree passed, and the audio was byte for byte identical across all eight voices, because the sentence they were checked against had no diphthong in it. A person heard dijous once and said it rhymed with nothing.

## A language module as text

Everything a language module holds now has a form a person can write, and each form regenerates the file the build compiles, byte for byte, for all eight languages.

The rules are `lang/<tag>/rules`, in the two forms `docs/building.md` describes. The words are `<tag>.dict`. The five beside them are `<tag>.globals` for the variables the machine declares, `<tag>.settings` for the settings the engine carries in its image, `<tag>.statements` for the statement table it is parameterised by, `<tag>.sets` for the lookup sets and the dictionary's actions, and `<tag>.consts` for the bytes the rules name by address. `make tables-check` writes the C out of each and holds it against the tree: 45 of 45 across nine languages, and it wants neither Wine nor IBM's objects.

Each of the five has one writer, shared by the lifter that reads IBM's objects and the reader that reads the text, so the two cannot drift into formatting differently. And nothing in a text is stated twice: the variables are runs of kinds and where each lands is walked from them exactly as `delta_new` walks it, the statement table's readers and writers are an offset and a width whose names follow the order of the fields, and the settings' language number is the section that names it.

Two measurements worth keeping. Every one of the eight languages declares ten statement types, with 57 fields in Italian and both Spanishes, 58 in the two Englishes, 61 in German, 63 in Canadian French and 65 in French. And the sets are where the size of a language sits: English declares 511 of them and 28 dictionary actions over 274 kilobytes of entries, where Italian declares 153 and 13 over 77.

The sets' text is written out of the C in the tree rather than out of IBM's objects, and that is deliberate: the dictionary's arrays in that file are laid down by `tools/delta-dict.py` out of the words, so the objects hold what the dictionary said before anything was added. Writing the text from the tree means the text carries the words as they stand.

That work also found that `tools/delta-sets.py` had not been able to write the file it generates for some time. The copy in the tree had been brought to the arena's forms during the sixty-four bit port and the tool had not, and its comment about the stores had gone stale with it. English's file had the newer forms and the other seven the older; the tool now writes English's and the seven have been brought into line, ten lines each with no data touched.

## Partly done

The rules read as rules, to sixteen passes of the decompiler. What that means concretely: calls sit with their arguments, wrapper rules say which primitive they stand for, state reaches say which language variable they touch, frame reaches say which argument they are, the arms of a backtracking dispatch say which alternative they are whichever of the two ways the compiler wrote the dispatch, register halves say which half, a test of the flags is the comparison the machine made -- a call's answer against nought, a length against a limit, a bit against a mask -- rather than a flag set and a flag read, letting go of a call's arguments says that and not that a scratch register was written, a jump at the return is a return, 2,391 loops are loops, a jump out of one says it is leaving it, the two places a rule ends say whether it has matched or given up, and the machine's dead leavings are gone.

What is left in them: 59,295 gotos, of which 26,668 are the arms of a backtracking dispatch and are right as they are, and 4,254 more say plainly that the rule has matched or has given up. Most of the rest are the same thing without a name, because that is what the language is: a pattern matcher whose every failure jumps to a shared tail.

Of the flags, 49 comparisons and 1,054 conditions are left out of the 24,140 and 27,013 there were. The comparisons are the ones with a label between them and the condition that reads them, where something else can arrive with other flags. The conditions are mostly reading what an arithmetic operation left rather than what a comparison did -- the decrement in a dispatch, whose zero flag says the answer was one -- and those are the next ones that could go.

`tools/delta-shape.py` says what more structuring could reach, and the answer is bounded. Of the 106,072 edges between the 53,439 basic blocks, 6,004 jump back to a block that does not stand on every path to them, which is the definition of flow that no arrangement of loops and conditionals can say. 610 of the 1,042 rules have at least one, 191 have ten or more, and the worst is `hebrew_ph_Q` with 129 in 186 blocks. Saying those in a structured language means copying the code the jump lands on, or adding a variable to dispatch on; the first costs the correspondence between the C and the bytecode, which is what makes the C checkable, and the second buries the dispatch the rules already have under an invented one.

Three things in the rules cannot be recovered and are not going to be. The global variables' names are gone: they are known only by kind and number, because the only record of them is a disassembly of `delta_new` that carries kinds and not names. The frame below a rule's arguments is unnamed, because nothing in the object says what any of it is for. And the 152 wrapper rules that do arithmetic as well as calling keep their names.

## Known limits

The suite is blind to the eight voices the caller may edit, and that was found by accident rather than by looking. Nothing it runs asks an instance about voice nine, so the loop that copies the language's own eight into them can be turned off and all 81 English cases, all 80 German and the samples hash still pass. A stale script in `/tmp`, left there on 17 August by a session doing the sabotage check, made exactly that edit on 23 August -- Python imported it under a standard module's name and ran it -- and nothing in the tree noticed. `make voices` is the answer and it runs in CI; `docs/building.md` says what it holds an instance to and which three sabotages it catches.


The suite compares one utterance per process, and until 22 August 2026 that was the only utterance anything had ever compared. It is no longer: `probe` and the reference both take a `t` in their mode argument, which says the same text a second time on the same instance and writes it beside the first.

What that found is worth knowing before reading any byte comparison here. The second utterance is not the first -- 38,423 samples both times, 30,495 of them different -- because the machine's state has moved on. That is faithful: IBM's own engine differs across its two utterances to the same 30,495 samples, and ours matches IBM's *second* utterance byte for byte as well as its first. And it is deterministic: three processes give the same first utterance and the same second one, to the hash.

So samples are comparable, a second utterance included, as long as both sides have spoken the same history; what cannot be compared is a second utterance against a first. An earlier note here said the engine produced a different hash each time, which was wrong -- the difference is between the first utterance and the second, not between one run and the next.


The test suite needs IBM's objects, because it compares against IBM's binary, and on anything but Windows it needs Wine to run that binary. Both are obtainable: `docs/building.md` says where IBM's SDK still is. Without them there is no automatic check that the audio is right, only `tools/say.sh` to listen with and `tools/delta-check.sh` to hold the two forms of a rule against each other.

That last one compares every rule entered and every call made, with their arguments, over the seven plain cases taken one at a time. Three kinds of line are left out and all three are the interpreter's alone: the stores it makes, which a rule written as C makes for itself; its remark about the argument area being a different depth than the compiled code expected; and addresses in the arena, which differ because a rule written as C takes a smaller frame on purpose.

Interrupting from another thread is answered, and `make stopthread` is what answers it. `eciStop` called from a second thread while the engine is handing samples over used to fault every time; with the busy guard it was none of twelve on real Windows and eight of twelve under Wine, and both of those were taken before the queue-count fix. On 23 August 2026, with both fixes in, twenty-four turns come through clean on Linux and twenty-four under Wine, every one of them stopping an engine that was still delivering, and every follow-up utterance worth exactly what a whole one is. Removing the busy guard faults both of them at once, which is what says the harness can see the door at all. Answering `eciDataAbort` from the callback is clean too: `make interrupt` holds twelve turns on Linux and twenty-five under Wine and on real Windows.

What that harness will not assert, and the reason is worth keeping, is that an interrupted utterance comes out short. A stop cannot make the engine abandon an utterance; all it can do is stop the rest of the buffers being handed over, so whether the sample count is short depends on where the suspension lands between two buffers. A callback that returns at once lets the engine finish before another thread reacts, and a callback that paces itself like a real player runs on the engine's own thread, so the stop then waits for the whole delivery and can never truncate. Both were tried. The count is reported and not required.

Two lessons were bought expensively on the way and are worth more than the numbers were. A library held open cannot be replaced, and a screen reader holds its own: an evening of "the abort door faults on Windows" turned out to be an add-on loading an `eci.dll` from its installed directory that had been locked open since before the fix, so every measurement was against the engine that still had the bug. Anything experimental gets its own staged copy of the library. And a fault with an empty stderr and 0xC0000005 says only that nothing in the engine complained; it does not say what happened, and it is not evidence for whichever mechanism looks like it.

A hazard was found while chasing that, and it is real on its own terms even though it explained nothing that was reported. A landing place jumped to from a thread that never planted it used to jump to nought. The engine backtracks to a place a rule planted, named by the address of the rule's frame; the frames are in the arena, which every thread shares, and the machine keeps the one it means to return to in its own state, where whoever stops the engine can reach it. So the name travels between threads although the landing place cannot: the table of places is one per thread, on the stated assumption that two threads cannot share a name, which the arena makes false. The lookup then made a place rather than refusing, the new place was all noughts, and the jump loaded nought as the stack pointer and went to nought.

The lookup now refuses, so the same mistake is a sentence rather than a fault with nothing in it. `make landing` is the check: it plants a landing and lands on it, then jumps to that name from another thread, and answers non-zero if the guard does not fire. Against the committed code that test is a bare segmentation fault. This does not make stopping from another thread correct -- it makes it say what is wrong, on every platform, at the moment it happens. Doing it properly means the stop asking the engine's own thread to unwind rather than unwinding it from outside, and that is not done. Nothing observed so far needs it: five drive loops on Linux -- polling, `eciSynchronize`, synthesis on a worker thread, the same with the cancelling thread polling, and `eciStop` from a second thread -- all run clean, and so does the add-on's own loop once it is loading a current library.

One narrow window is left in the guard and is not closed. A place is marked as planted before the save writes into it, so another thread jumping in between the two would find a planted but empty place and fault as it did before. Closing that means marking after the save returns, which is awkward across a call that returns twice and across the thirty-two bit `setjmp` path. The window only exists while a cross-thread jump is already happening, which is the mistake being diagnosed.

What cancelling costs, measured in the engine. Every figure here is from `build/probe` on Linux with no player in the picture at all; the numbers taken through the add-on on Windows have been withdrawn by the session that took them, because the fake player in that harness paced a buffer by sleeping for its duration and a cancel did not reliably interrupt the sleep, so most of a buffer was being counted as engine time. What that harness said about *relative* improvement was measured the same way on both sides and is probably sound; its absolute numbers are not, and none of them are quoted here.

Cancelling costs the same by all three routes -- letting the utterance finish and throwing its samples away, answering `eciDataAbort`, and calling `eciStop` from the cancelling thread -- because all three wait for the same thing. That was measured on Windows too and is a comparison rather than an absolute, so it survives the withdrawal above.

So the abort door being open does not help the latency, and the earlier claim here that what remained was the add-on removing its workaround was an inference from the door opening rather than a measurement. It is withdrawn. Answering `eciDataAbort` stops the engine handing over more buffers; it does not stop it finishing the utterance, and neither does `eciStop`.

**The engine cannot abandon an utterance it has been told to stop, and that is settled rather than open.** All of a cancel is `stm_qtSuspend` waiting for the synthesis thread to finish the message it is on: 71 ms of 71 for a long row, where re-initialising the language globals is 0.3 ms and suspending the queues is nought. The machine is never told to stop -- `ELOQ_BUSY` is set every time that branch is reached, so `throwDeltaErrorNow` is never called -- and the walk does exactly the work that was left: a row is 298,745 rule entries, 97,382 before the first buffer and 201,411 after the stop, and the two add up. The cooperative interrupt that is raised is honoured 123 times in the sample generators and is worth about 20 ms of that.

Six ways of making it give up were tried and all fail, and the reason is one thing rather than six. The best of them had the decompiler emit, just after each rule is entered, a jump to that rule's own give-up tail -- the one that calls `vretproc` and puts back everything the rule's record holds -- in all 459 rules that have such a tail. It faults in `vdef_proj` under `vrange_2pt` under `mark_s`, from a rule that ran *after* the one that gave up. **The rules build a shared structure as they go and later rules assume the earlier ones finished it**, so abandoning any rule part way leaves that structure half built and nothing validates it. There is no safe abandonment point anywhere in the machine, and the only safe stop is to let the unit of work finish -- which is what `stm_qtSuspend` already does. Making the machine interruptible would mean the rules checking their own inputs, which is the language and not a patch.

What that leaves is doing the leftover work faster, which is why the rules are now compiled by default. Measured on Linux, bytecode against `RULES=c`: the row synthesises in 138 ms against 63, the wait before the first samples is 38 ms against 12, and cancel-and-speak is 124 ms against 39. So the cancel cost falls from about 86 ms to about 27, and a filename from 22 ms to 8. Nothing was written to achieve that; it is the same rules, compiled.

A trap in the interface, found while chasing a residual that turned out not to be the engine's at all, and worth knowing on its own terms. A caller answering `eciDataNotProcessed` -- "I did not take these samples" -- costs a flat thirty milliseconds per buffer: `eo_tell` in `src/eci_old.c` maps it to `BRIDGE_ABORT`, which is `APP_AGAIN`, and `aq_synchronize` answers that with `th_sleep(30, 0)` before offering the same buffer again. That is right for a player whose buffer is genuinely full and ruinous for a caller that is discarding, and nothing in the interface warns of it. A caller that means "throw this away" must answer `eciDataProcessed`. This is IBM's behaviour and is not being changed.

Interrupting an utterance and then speaking again on the same instance used to leave the engine quiet from the second interruption onwards, accepting the text, reporting no error, answering that it was not speaking, and saying nothing. That is fixed. `make interrupt` is the check and it now asserts what it used to only print: twelve turns, every follow-up utterance worth exactly what the first whole one was.

It was never on the interrupt path. The application queue keeps two counts, one of everything it has been told about and one of everything the application has collected, and it takes them being equal to mean there is nothing outstanding. The stop put the first back to nought and left the second where it was, so the two drifted; when the stale one happened to equal the fresh one, the queue believed it had caught up and handed over none of the samples already sitting in it. The text had arrived and the samples had been made. They were made and then not collected.

Only the sixty-four bit builds had it, which is why the suite never saw it: the stop reached that second count by the byte it sat at when a pointer was four bytes, and at sixty-four bits that byte is inside the queue's own send lock. The same build thirty-two bit runs the check clean without the fix. That is the third time an unconverted byte offset has cost a fault, so the offsets that reach a block by number rather than by name were swept afterwards; what the sweep found is below.

The sweep, one fixed and one left. Eighteen blocks in the engine are still reached by the byte a field sat at. Most are safe and stay that way for a reason: a block the machine can see has the same layout in both bitnesses by design, because everything in it that points is a four-byte `evv_ref` rather than a pointer, so a number into one of those means the same thing either way. What is not safe is reading such a field as a pointer, or reaching into a block whose layout is ours and has grown. Two of the eighteen do one of those.

`src/eci_deltalib.c` writes the machine's "undefined reads back as this" field as a `const char **`, and what it writes is the address of `"---"` in the program -- or it did, and this one is fixed. Both halves were wrong at sixty-four bits: the field is a four-byte reference into the arena, so an eight-byte write spills past it, and an address in the program is the one thing the machine may never be given. Going through the raw offset is exactly what slips it past `evv_ref_checked`, which exists to refuse this and never gets the chance. The reader, in `src/eci_access.c`, takes it as a reference and translates it, so at sixty-four bits it translates rubbish; at thirty-two the two agreed by accident, because a reference and a pointer are the same four bytes there. Nothing asks for it in the 81 cases, so it was latent rather than live -- but reading that field back and using it as a string faults, which is a measurement and not an argument. The string is now copied into the arena by `delta_low_copy` and the field holds a reference to the copy, which reads back as "---".

`src/eci_pcm16.c` and `src/eci_soundfmt.c` reach a sound file's block by offsets that overlap once a pointer is eight bytes: the format at 0x0c runs over the rate at 0x10, the stream at 0x18 over how it was opened at 0x1c, and the index function at 0x20 over its parameter at 0x24. That block is ours, not the machine's, so it should be a struct -- and `src/eci_soundfile.c` already declares that struct and already has the right idiom for the format table, `FMT_SLOT`, which reads a slot as the byte offset divided by four. Three things say the layer is unreachable rather than merely unused, so this is left alone and written down instead. `ST_SOUND(t)` is nought, measured by printing it: every sender in the engine is guarded by it, so nothing in the sound thread runs. `ealAudioSoundFormat` is `int32_t[16]`, sixteen words of nought, and it registers first and is therefore the default format, so nothing could be called through it. And `constructSoundFile` and `soundFileFormat` have no callers anywhere in the tree. Since nothing can observe that code, nothing can check a change to it either, which is the reason for leaving it: a fix there would be mechanically right and unverified. Two things to know when it is wired up. `FMT_SLOT` itself is wrong -- `(*(void ***)(f))[(off) / 4]` has one dereference too many for a table that is the array, so it would index off the format's name -- and the six offsets `eci_soundfile.c` names all land on the right function when read as slots, which is what says the layout is determined rather than guessed. It becomes live the day anything asks the engine to write a file itself, or the day Linux audio is wired through this layer rather than through the buffer.

Making and throwing away engine instances used to leak four megabytes each, and the engine went quiet on the 63rd: it had run out of the arena, the region below two gigabytes that everything the machine can hold a pointer to comes out of. That is fixed, and `make instances 200` now runs two hundred rounds all owing the same samples where it used to stop at 63.

It was the frame stack. A rule hands the machine the address of its own frame, so frames cannot be ordinary locals and come from a four megabyte stack the thread takes on the first rule it runs -- kept for the life of the thread, which is right, since the frames nest strictly and the stack is reused. What was wrong is that nothing gave it back. Every instance starts a synthesis thread, so every instance kept four megabytes, and 256 divided by four is 62. Both trampolines in the porting layer now call `evv_frame_done` once the thread body has returned, which is the one moment it is certainly safe: no rule of that thread is running and nothing holds a frame.

`evv_arena_outstanding` is what found it and is now in the tree. It walks the arena and reports what is still held, grouped by the allocation that asked and the size it got, heaviest first. A leak is the group that grows by the same amount with every instance: this one was one block of 4,194,320 bytes per instance, against everything else standing still, which named it in a single run. `whence` is the low thirty-two bits of a return address; build with `make CFLAGS=-no-pie` to turn one into a line with addr2line.

What is left is two blocks of thirty-two bytes per instance from one allocation site, which the same report shows and nobody has chased. Sixty-four bytes an instance moves the ceiling from sixty-two instances to about four million, so it is a real leak that no caller will meet.

The number is 63 in both forms of the rules, which says the leak is in the engine rather than in either rule table. What differed until 22 August 2026 was what happened at that point: the interpreter answers nought and the engine goes quiet, because `run_bytecode` checks whether it got a frame, while a rule written as C went straight to `memset` on the nought it had been handed and faulted. So exhausting the arena crashed the compiled build and merely silenced the interpreted one. The decompiler now emits the same check the interpreter has, and both answer nought.

That is worth knowing beyond this one fault: the two rule forms are held against each other over the 81 cases and by `tools/delta-check.sh`, and neither of those speaks through more than one instance, so a difference that only shows when something runs out was invisible to both. `make instances` is the only check that makes a second instance at all.

One thing the trace check turned up is a fault of ours, and is not fixed. Tracing at the level that prints every call costs twenty times what the synthesis does, and feeding the synthesis that slowly faults part way through a run of several sentences, with less audio written than there should be. Any one sentence is fine. Nothing but a trace makes the engine that slow, so it is not in the way of anything, but it is a fault and this is where it is written down.

The reference binary is not steady on a case with a marker in it. Usually it hangs, and `test/compare.sh` retries a case once on its own before reporting it as hung, because calling that a difference cost false alarms. Sometimes it answers with different samples instead, which the retry cannot tell from a real difference: six runs of one German audio-marker case gave the same samples from ours every time and a different one once from IBM's. A marker case that differs once and not again is that, not a change in the engine.

The sixty-four bit build maps a region low in memory, because the Delta machine keeps addresses in thirty-two bit values and everything it can point at has to be nameable in one. The program itself may be loaded anywhere: the language's data is copied into that region at startup rather than named where it lies. A machine that cannot map anything below two gigabytes would need a different answer, and would say so rather than misbehave.

If the audio sounds wrong to you, it is not a fault in the port: our output is identical to IBM's. Changing it is a deliberate change to the language data, and the suite will correctly report that as a difference.
